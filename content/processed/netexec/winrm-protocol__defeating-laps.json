{"title": "Defeating LAPS", "source": "netexec", "source_label": "NetExec Wiki", "path": "winrm-protocol/defeating-laps", "html": "<hr />\n<h2 id=\"description-netexec-vs-laps\">description: NetExec vs LAPS</h2>\n<h1 id=\"defeating-laps\">Defeating LAPS</h1>\n<h3 id=\"using-netexec-when-laps-installed-on-the-domain\">Using NetExec when LAPS installed on the domain</h3>\n<p>If LAPS is used inside the domain, is can be hard to use NetExec to execute a command on every computer on the domain.</p>\n<p>Therefore, a new core option has been added <code>--laps</code>! If you have compromised an accout that can read LAPS password you can use NetExec like this</p>\n<div class=\"highlight\"><pre><span></span><code>nxc<span class=\"w\"> </span>winrm<span class=\"w\"> </span>&lt;ip&gt;<span class=\"w\"> </span>-u<span class=\"w\"> </span>user-can-read-laps<span class=\"w\"> </span>-p<span class=\"w\"> </span>pass<span class=\"w\"> </span>--laps\n</code></pre></div>\n<p>{% hint style=\"info\" %}\nIf the default administrator name is not \"administrator\" add the user after the option</p>\n<p _=\"%\" endhint=\"endhint\"><code>--laps name</code></p>", "excerpt": "description: NetExec vs LAPS Defeating LAPS Using NetExec when LAPS installed on the domain If LAPS is used inside the domain, is can be hard to use NetExec to execute a command on every computer on t", "tags": []}