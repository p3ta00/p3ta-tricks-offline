{"title": "ssh-tunneling", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-pivoting-ssh-tunneling", "html": "<h1 id=\"pivoting-ssh-tunneling\">ssh-tunneling</h1>\n<p>&gt; Built-in pivoting through any SSH access. No tools to upload. Use when you have SSH creds on a dual-homed host.</p>\n<h3>Local Port Forward</h3>\n<p>Forward a remote port to your local machine:</p>\n<pre><code class=\"language-bash\"># Access internal_host:80 via localhost:8080\nssh -L 8080:internal_host:80 user@pivot\n\n# Access MSSQL on internal host\nssh -L 1433:10.10.10.5:1433 user@pivot</code></pre>\n<h3>Remote Port Forward</h3>\n<p>Expose your local port on the remote host:</p>\n<pre><code class=\"language-bash\"># Make your port 9001 accessible on pivot:9001\nssh -R 9001:127.0.0.1:9001 user@pivot</code></pre>\n<h3>Dynamic SOCKS Proxy</h3>\n<pre><code class=\"language-bash\">ssh -D 1080 user@pivot\n# Then use proxychains with socks5 127.0.0.1 1080</code></pre>\n<h3>Jump Hosts</h3>\n<pre><code class=\"language-bash\">ssh -J user@jump user@target\nssh -J user1@jump1,user2@jump2 user@target</code></pre>\n<h3>ProxyCommand (persistent config)</h3>\n<pre><code class=\"language-bash\"># ~/.ssh/config\nHost internal\n    HostName &lt;target-ip&gt;\n    User admin\n    ProxyJump user@pivot</code></pre>\n<h3>Tips</h3>\n<p>- Add <code>-N</code> to not open a shell (just tunnel) - Add <code>-f</code> to background the SSH process - Use <code>autossh</code> for persistent tunnels</p>", "excerpt": "ssh-tunneling &gt; Built-in pivoting through any SSH access. No tools to upload. Use when you have SSH creds on a dual-homed host. Local Port Forward Forward a remote port to your local machine: # Acc…", "tags": ["reference", "pivoting"]}