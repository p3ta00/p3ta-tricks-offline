{"title": "nuclei", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-enumeration-nuclei", "html": "<h1 id=\"enumeration-nuclei\">nuclei</h1>\n<p>Fast scanner using community-maintained YAML templates. Good for quick vuln checks after initial recon surfaces web services.</p>\n<h3>Basic Scanning</h3>\n<pre><code class=\"language-bash\"># Auto-detect technology and scan with relevant templates\nnuclei -u http://target -as\n\n# Scan single URL\nnuclei -u http://target\n\n# Scan from URL list\nnuclei -l urls.txt\n\n# Scan with automatic browser-based crawling\nnuclei -u http://target -headless</code></pre>\n<h3>Template Selection</h3>\n<pre><code class=\"language-bash\"># Specific template category\nnuclei -u http://target -t cves/\nnuclei -u http://target -t exposures/\nnuclei -u http://target -t misconfigurations/\nnuclei -u http://target -t default-logins/\nnuclei -u http://target -t vulnerabilities/\nnuclei -u http://target -t technologies/\n\n# Specific template\nnuclei -u http://target -t cves/2021/CVE-2021-44228.yaml\n\n# Multiple categories\nnuclei -u http://target -t cves/ -t exposures/ -t misconfigurations/\n\n# Exclude templates\nnuclei -u http://target -exclude-templates dos/\n\n# Custom template directory\nnuclei -u http://target -t /path/to/custom-templates/</code></pre>\n<h3>Severity Filtering</h3>\n<pre><code class=\"language-bash\"># Critical and high only\nnuclei -u http://target -severity critical,high\n\n# Exclude info-level noise\nnuclei -u http://target -exclude-severity info\n\n# Single severity\nnuclei -u http://target -severity critical</code></pre>\n<h3>Tag Filtering</h3>\n<pre><code class=\"language-bash\"># By technology tag\nnuclei -u http://target -tags apache\nnuclei -u http://target -tags wordpress\nnuclei -u http://target -tags joomla\nnuclei -u http://target -tags rce\n\n# Exclude tags\nnuclei -u http://target -exclude-tags dos,fuzz\n\n# Common useful tags\n# rce, lfi, xss, sqli, ssrf, redirect, exposure, token, default-login, cve</code></pre>\n<h3>Rate Limiting &amp; Performance</h3>\n<pre><code class=\"language-bash\"># Rate limit (requests per second)\nnuclei -u http://target -rl 50\n\n# Concurrent templates\nnuclei -u http://target -c 25\n\n# Bulk size (hosts processed in parallel)\nnuclei -u http://target -bs 50\n\n# Timeout per request (seconds)\nnuclei -u http://target -timeout 10\n\n# Retries\nnuclei -u http://target -retries 3</code></pre>\n<h3>Output</h3>\n<pre><code class=\"language-bash\"># Save to file\nnuclei -u http://target -o results.txt\n\n# JSON output\nnuclei -u http://target -jsonl -o results.json\n\n# Markdown report\nnuclei -u http://target -me report/\n\n# Silent (only results)\nnuclei -u http://target -silent</code></pre>\n<h3>Authentication &amp; Headers</h3>\n<pre><code class=\"language-bash\"># Custom headers\nnuclei -u http://target -H &quot;Authorization: Bearer TOKEN&quot;\nnuclei -u http://target -H &quot;Cookie: session=abc123&quot;\n\n# Proxy\nnuclei -u http://target -proxy http://127.0.0.1:8080</code></pre>\n<h3>Template Management</h3>\n<pre><code class=\"language-bash\"># Update templates\nnuclei -update-templates\n\n# List templates\nnuclei -tl\n\n# Template stats\nnuclei -stats</code></pre>\n<h3>HTB Common Combos</h3>\n<pre><code class=\"language-bash\"># Quick vuln check after nmap finds web\nnuclei -u http://target -severity critical,high,medium -o nuclei_results.txt\n\n# Full auto-scan (technology detection + relevant templates)\nnuclei -u http://target -as -severity critical,high\n\n# Scan multiple discovered endpoints\ncat urls.txt | nuclei -severity critical,high -o findings.txt\n\n# Check for default creds\nnuclei -u http://target -t default-logins/ -o defaults.txt\n\n# Check exposed files and configs\nnuclei -u http://target -t exposures/ -severity medium,high,critical\n\n# CVE-specific check\nnuclei -u http://target -tags cve -severity critical</code></pre>\n<h3>Workflow with Other Tools</h3>\n<pre><code class=\"language-bash\"># Pipe from httpx (probe alive hosts first)\ncat subdomains.txt | httpx -silent | nuclei -severity critical,high\n\n# After ffuf finds endpoints\ncat ffuf_results.json | jq -r &#x27;.results[].url&#x27; | nuclei</code></pre>", "excerpt": "nuclei Fast scanner using community-maintained YAML templates. Good for quick vuln checks after initial recon surfaces web services. Basic Scanning # Auto-detect technology and scan with relevant temp…", "tags": ["reference", "enumeration"]}