{"title": "ldapsearch", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-enumeration-ldapsearch", "html": "<h1 id=\"enumeration-ldapsearch\">ldapsearch</h1>\n<p>&gt; Raw LDAP queries for AD enumeration. Use when you need specific attributes or BloodHound misses edge cases.</p>\n<h3>Base Queries</h3>\n<pre><code class=\"language-bash\"># Anonymous bind - get naming contexts\nldapsearch -x -H ldap://DC_IP -b &#x27;&#x27; -s base namingContexts\n\n# Authenticated bind\nldapsearch -x -H ldap://DC_IP -D &#x27;user@domain.htb&#x27; -w &#x27;pass&#x27; -b &#x27;DC=domain,DC=htb&#x27;</code></pre>\n<h3>User Enumeration</h3>\n<pre><code class=\"language-bash\"># All users with key attributes\nldapsearch ... -b &#x27;DC=domain,DC=htb&#x27; &#x27;(objectClass=user)&#x27; sAMAccountName memberOf description userAccountControl adminCount servicePrincipalName\n\n# Users with SPNs (Kerberoastable)\nldapsearch ... &#x27;(&amp;(objectClass=user)(servicePrincipalName=*))&#x27; sAMAccountName servicePrincipalName\n\n# AS-REP roastable (no preauth required)\nldapsearch ... &#x27;(userAccountControl:1.2.840.113556.1.4.803:=4194304)&#x27; sAMAccountName\n\n# Disabled accounts\nldapsearch ... &#x27;(userAccountControl:1.2.840.113556.1.4.803:=2)&#x27; sAMAccountName</code></pre>\n<h3>Computer Enumeration</h3>\n<pre><code class=\"language-bash\">ldapsearch ... &#x27;(objectClass=computer)&#x27; dNSHostName operatingSystem managedBy msDS-AllowedToActOnBehalfOfOtherIdentity</code></pre>\n<h3>Group Enumeration</h3>\n<pre><code class=\"language-bash\"># Recursive membership\nldapsearch ... &#x27;(member:1.2.840.113556.1.4.1941:=CN=User,CN=Users,DC=domain,DC=htb)&#x27; dn</code></pre>\n<h3>Delegation</h3>\n<pre><code class=\"language-bash\"># Constrained delegation\nldapsearch ... &#x27;(msDS-AllowedToDelegateTo=*)&#x27; sAMAccountName msDS-AllowedToDelegateTo\n\n# Unconstrained delegation (excluding DCs)\nldapsearch ... &#x27;(&amp;(userAccountControl:1.2.840.113556.1.4.803:=524288)(!(userAccountControl:1.2.840.113556.1.4.803:=8192)))&#x27; sAMAccountName\n\n# RBCD\nldapsearch ... &#x27;(msDS-AllowedToActOnBehalfOfOtherIdentity=*)&#x27; sAMAccountName</code></pre>\n<h3>Special Attributes</h3>\n<pre><code class=\"language-bash\"># LAPS passwords\nldapsearch ... &#x27;(objectClass=computer)&#x27; ms-Mcs-AdmPwd msLAPS-Password\n\n# gMSA accounts\nldapsearch ... &#x27;(objectClass=msDS-GroupManagedServiceAccount)&#x27; sAMAccountName msDS-GroupMSAMembership\n\n# RODC revealed users\nldapsearch ... &#x27;(objectClass=computer)&#x27; msDS-RevealedUsers msDS-NeverRevealGroup msDS-RevealOnDemandGroup</code></pre>", "excerpt": "ldapsearch &gt; Raw LDAP queries for AD enumeration. Use when you need specific attributes or BloodHound misses edge cases. Base Queries # Anonymous bind - get naming contexts ldapsearch -x -H ldap://…", "tags": ["reference", "enumeration"]}