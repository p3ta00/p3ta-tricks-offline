{"title": "feroxbuster", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-enumeration-feroxbuster", "html": "<h1 id=\"enumeration-feroxbuster\">feroxbuster</h1>\n<p>Aggressive recursive content discovery. Automatically follows directories it finds and scans deeper. Use when you need thorough coverage.</p>\n<h3>Basic Usage</h3>\n<pre><code class=\"language-bash\"># Default scan\nferoxbuster -u http://target\n\n# With wordlist\nferoxbuster -u http://target -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt\n\n# With extensions\nferoxbuster -u http://target -w /usr/share/seclists/Discovery/Web-Content/raft-medium-words.txt -x php,asp,aspx,html,txt,bak,old,json,xml</code></pre>\n<h3>Recursion Control</h3>\n<pre><code class=\"language-bash\"># Set recursion depth (default: 4)\nferoxbuster -u http://target --depth 2\n\n# No recursion (single level only)\nferoxbuster -u http://target --no-recursion\n\n# Don&#x27;t recurse into specific dirs\nferoxbuster -u http://target --dont-scan /assets/ --dont-scan /images/</code></pre>\n<h3>Filtering Responses</h3>\n<pre><code class=\"language-bash\"># Filter by status code\nferoxbuster -u http://target --filter-status 404,403,500\n\n# Filter by response size (bytes)\nferoxbuster -u http://target --filter-size 0\n\n# Filter by word count\nferoxbuster -u http://target --filter-words 42\n\n# Filter by line count\nferoxbuster -u http://target --filter-lines 10\n\n# Filter by regex in response body\nferoxbuster -u http://target --filter-regex &quot;Page Not Found&quot;\n\n# Only show specific status codes\nferoxbuster -u http://target --status-codes 200,301,302</code></pre>\n<h3>Speed &amp; Performance</h3>\n<pre><code class=\"language-bash\"># Thread count (default: 50)\nferoxbuster -u http://target -t 100\n\n# Rate limit (requests per second)\nferoxbuster -u http://target --rate-limit 50\n\n# Timeout per request (seconds)\nferoxbuster -u http://target --timeout 10</code></pre>\n<h3>Output</h3>\n<pre><code class=\"language-bash\"># Save to file\nferoxbuster -u http://target -o results.txt\n\n# JSON output\nferoxbuster -u http://target -o results.json --json\n\n# Silent mode (less terminal noise)\nferoxbuster -u http://target --silent</code></pre>\n<h3>Authentication &amp; Headers</h3>\n<pre><code class=\"language-bash\"># Cookie auth\nferoxbuster -u http://target -b &quot;session=abc123&quot;\n\n# Custom headers\nferoxbuster -u http://target -H &quot;Authorization: Bearer TOKEN&quot;\n\n# Proxy through Burp\nferoxbuster -u http://target -p http://127.0.0.1:8080\n\n# Insecure (skip TLS verify)\nferoxbuster -u https://target -k</code></pre>\n<h3>Scan Control</h3>\n<pre><code class=\"language-bash\"># Force scan even on wildcard responses\nferoxbuster -u http://target --force-recursion\n\n# Add trailing slash to requests\nferoxbuster -u http://target --add-slash\n\n# Follow redirects\nferoxbuster -u http://target -r\n\n# HTTP methods\nferoxbuster -u http://target -m GET,POST\n\n# User-agent\nferoxbuster -u http://target -a &quot;Mozilla/5.0 (Windows NT 10.0; Win64; x64)&quot;</code></pre>\n<h3>Resume &amp; State</h3>\n<pre><code class=\"language-bash\"># Auto-saves state file on ctrl+c / crash — resume with:\nferoxbuster --resume-from ferox-state.json</code></pre>\n<h3>HTB Common Combos</h3>\n<pre><code class=\"language-bash\"># Thorough PHP scan\nferoxbuster -u http://target -w /usr/share/seclists/Discovery/Web-Content/raft-medium-words.txt -x php,phps,php.bak -t 80 --depth 3\n\n# Quick look\nferoxbuster -u http://target -w /usr/share/wordlists/dirb/common.txt --no-recursion\n\n# Through Burp with extensions\nferoxbuster -u http://target -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -x php,txt,html -p http://127.0.0.1:8080</code></pre>\n<h3>Common Wordlists</h3>\n<pre><code class=\"language-bash\">/usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt   - general directories\n/usr/share/seclists/Discovery/Web-Content/raft-medium-words.txt         - general words\n/usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt - classic\n/usr/share/wordlists/dirb/common.txt                                    - quick common paths\n/usr/share/seclists/Discovery/Web-Content/big.txt                       - thorough</code></pre>", "excerpt": "feroxbuster Aggressive recursive content discovery. Automatically follows directories it finds and scans deeper. Use when you need thorough coverage. Basic Usage # Default scan feroxbuster -u http://t…", "tags": ["reference", "enumeration"]}